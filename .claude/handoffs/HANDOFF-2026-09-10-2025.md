# Session Handoff: ET-Miner, 2026-09-10 20:25

**Branch:** `council-session-blockers` @ `e70225e` (no upstream)
**Tree:** 5 modified, 5 untracked, 0 stashes — **4 of the modified files are ANOTHER SESSION's live work, not this session's**
**Focus:** PR 6 remediation. Third `/council review PR6` returned 4 BLOCK; remedy is specified but NOT implemented.
**Reason:** User asked to terminate the council agents and hold until they say "proceed"

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `e70225e`. The tree will NOT be clean and that is expected — see "Blockers". If HEAD moved, read the diff before trusting "Current State".

## Current State
- **Nothing is being edited. No work is in progress in any file by this session.**
- Three councils have now run on PR 6. The third reviewed `f25fee9..e70225e` and returned **4 BLOCK / 0 APPROVE**. All four council agents were terminated on user instruction at 20:24 — their findings survive ONLY in this conversation and in this file.
- User's last instruction, verbatim intent: terminate the council agents, wait until they say **proceed**, because another Claude session is still working in this repo.
- Last commands: `TaskStop` × 4 (architect-4, math-4, alternatives-4, auditor-4) — all "Successfully stopped".

## Next Steps
1. **Wait for the user to say "proceed".** Do not edit files before that.
2. When cleared, implement the remedy below. User preference recorded: they were offered wait / proceed / partial and chose to wait for the other session to finish, so the whole remedy lands at once.
3. Re-run the gate set, then a fourth `/council review PR6` (user-invoked only — `/council` is `disable-model-invocation`).

## Session Instructions
- **Terminate council agents and hold until "proceed"** — the standing instruction.
- Earlier this session: "fix them" (twice) meant implement the council's flags in full, including grounds the user had not individually approved.
- `/council` cannot be launched from a session; ask the user to run it.
- Never `git checkout <file>` to undo an experiment in this repo — it has silently discarded uncommitted work. Use a scratchpad copy.

## Decisions & Rationale
- **Stopped both councils at round 2 of 3**, deviation stated not hidden: the panel converged completely on grounds and remedy, so round 3 could not change the outcome. Same precedent as the prior council.
- **Committed in per-ground pieces** rather than one commit, so a bisect lands on one defect at a time.
- **`numpy>=2.0` went in the `dev` group, not `[project].dependencies`** — `np.bitwise_count` is used only in `bench/repro/d63`, and `src/` uses no NumPy-2-only and no NumPy-2-removed API (both greps clean).
- **Campaign output nested under `bench/results/campaign/<rev>/`** rather than `campaign-<rev>/`: the former path is already gitignored, so no `.gitignore` edit — which would have swept up the user's pre-existing `.claude/` line.

## Dead Ends
- Do not fix `loader.py:133-135` by changing "three" to "four". Four reviewers independently confirmed "four" and were all wrong: the sentence quantifies over two sets and reports one number. "Four" is a true count of `_assert_home` call sites attached to a predicate FALSE for two of them (the multi-GPU wrappers pin per worker and pin only the decode/merge tail). Landing it would be WORSE than "three" — the number survives inspection while the clause stays false.
- Do not key the campaign directory by mode (`campaign/<rev>/<mode>`). It silences the only cross-mode refutation: smoke's `stressk2ml2-{legacy,shared}-2g` and full's `stressk2-filter-{compact,cupy,cpu}` all share `group_key == ('stress_k2', 2, None, False)`, and that is the sole place KERNEL_VARIANT and FILTER_IMPL results ever meet. Scoping `bad`/`stale` to `all_ids` covers everything the split was for.
- Do not scope `check_equivalence` to `all_ids` "for symmetry". Refute wide, claim narrow — scoping it suppresses the divergence above. Needs a comment naming that group explicitly, or a later prose-trimming pass will delete it.
- Do not pin the row_split peak with VmHWM. Below N≈10M the model stops being TRUE, not merely noisy (glibc does not return sub-mmap-threshold blocks).
- Do not pin it with a `np` namespace recorder either. `.copy()` defeats it with a byte-exact correct total (2 allocations == 8N+R, green) while the true peak is 1.702×.
- Do not use `np.arange` for the offsets fill if PR 6c takes that route: the `_chunk_start` must be hoisted ABOVE `_item_pos += _a.size`, or every row boundary shifts by one chunk and PyArrow accepts a structurally valid `LargeListArray` that reads the wrong rows.

## Key Findings
- **`row_split.py:903`'s `old = 16N` is not an identity** — it is the k̄=5 fixture value. Real form `12N + max(4N, 2R)`; `16N` only when k̄ ≥ 4. Measured at N=200M: k̄=3 → 3.251 vs 2.980; k̄=1 → 5.238 vs 2.980; real K=1..6 lattice (k̄=2.365) → 1.536 vs 1.291. `saving = 4N − R` is 5.5× low on the real lattice and **sign-inverted at k̄<2**. Correct: `saving = max(4N − R, R) >= R > 0` — strictly stronger, the change can never regress.
- **The block refutes itself from its own premises**, adjudicable by reading: `:882-883` states the `widths` pair "both live" (2R on top of 12N); `:909-910` asserts 27 lines later that the old peak precedes `widths` existing at all.
- **`runner.py:285` vs `:263-270`** — `check_equivalence` consumes unfiltered `rows`; `replayed` fills post-filter. So `--only` prints a bare ✓ where the PRE-commit code printed ✓ + REPLAY WARNING. Adding a filter erases the warning, in the commit written to stop false ticks.
- **`runner.py:333` vs `:298-300`** — all-crashed matrix returns 0 while the comment two lines up asserts "exit 0 IS the gate passing". `run_smoke.sh` runs under `set -euo pipefail`, so exit 0 IS the gate passing.
- **`bench/README.md:32`'s "re-run smoke if needed" can never tick** — six rows replay, `covered` requires `not replayed`. The documented quickstart's second run is broken at any revision.
- **`mining.py:974` — `cp.where(freq_mask)[0].astype(cp.int32)`.** `cp.where` returns int64 natively. That single hand-written, untested, unguarded cast defends the dtype of the entire gpu-resident chain (also feeds `:981`'s K=1 array and `:1010`'s K=2 dispatch). Measured on-device: an int64 `prev_freq_gpu` returns **56 itemsets of `[[0,0,0]]` at uniform count 46, no exception**, on an entry point exported at `kernels/__init__.py:62`.
- **`perf.md:3` and `:117`** still assert the baseline is "pinned at `22cb1dc` + PR 0" against this PR's own correction at `:93-97`. The `twophase-deepk` exemption's CONCLUSION survives (`git merge-base --is-ancestor 865286c 2eb81b0` is true, artifact holds 10752) — fix the premise, do not re-open the exemption.
- **`output_dir=""` writes to the FILESYSTEM ROOT** (filed as N22 in `BUGS_FOUND.md`). `io/gcs.py::join_gs_uri("")` synthesises a leading slash. Reachable from `apriori()`; threshold defaults to 100M rows so ordinary calls hit the root branch only.

## Blockers & Pending Decisions
- **BLOCKED ON USER: say "proceed".** Nothing may be edited until then.
- **Another Claude session has live uncommitted work in this repo** and is still writing (`tests/test_rules.py` and `tests/test_prune_observer.py` appeared after 20:22): `src/et_miner/core/prune_observer.py` (new), `core/apriori.py` (+44, a `witnesses` sink), `core/rules.py` (+50, a `conviction` metric), `tests/test_rules.py` (+106), and **`gpu/row_split.py` (+18)** — a `prune_observer=` param and `emit_gpu_level` call at ~`:737-755`. That last file is one the remedy edits, at `:901-912`, a different region. Leave it alone; do not revert or stash it.
- Reviewer harnesses are in the session scratchpad `/tmp/claude-0/-root-projects-ET-Miner/7e5550a9-c705-4291-8364-6ae9137e8382/scratchpad/` (`peak.py`, `tm2.py`, `tm3.py`, `alias.py`, `perchunk.py`, `allocpin.py`, a numpy 1.26.4 venv). **They will not survive the session** — copy them out if wanted.

## Test Status
All green at `e70225e`, independently re-run by two council reviewers:
- `uv run pytest -q`: **650 passed, 6 skipped** (was 648/6 before `c00786d`)
- `uv run pytest -q tests/test_tier_equivalence.py`: 10 passed
- `uv run ruff check src tests bench`: clean
- `uv run python bench/selfcheck.py`: READY, 19 kernels, 2 devices
- `uv run python bench/repro/run_all.py`: 13 live, 11 fixed, 0 broken
- `bash bench/run_smoke.sh`: exit 0, 6 run fresh, ✓ emitted
- `perf_baseline.py --compare`: hash `same` 10/11; `smoke-cpu-polars +11.4%` re-measured at 7 repeats → −3.3% (sub-second noise, confirmed by a reviewer at 4 more re-measurements)

## What Was Done
- Implemented the SECOND council's full remedy: `4344be8` (K=2 pin + `_assert_home` rewrite), `d013333` (the replay-vs-fresh gate), `f25fee9` (narration).
- Implemented its follow-up grounds: `c00786d` (guard order + `ndim!=2` + pin-claim scoping), `ee23ebd` (tick gated on full fresh coverage + per-revision `--out`), `e70225e` (memory identity + figures).
- Appended Section 4 to the untracked `BUGS_FOUND.md`: N20 (fixed), N21, N22.
- Ran the third `/council review PR6`; relayed and verified its findings; terminated all four agents.

## Commits This Session
- `e70225e` docs: state what the memory figures are conditional on, not just what they are
- `ee23ebd` fix: emit the equivalence tick only when it is a statement about this revision
- `c00786d` fix: order the K>=3 guards so the readable error can fire, and scope the pin claim
- `f25fee9` docs: close the council's narration flags, including two it found in the sweep
- `d013333` fix: the smoke gate printed "consistent" from a replay of pre-PR results
- `4344be8` fix: pin the K=2 single-GPU launch to its inputs -- N20's untreated twin
- `777287c` docs: finish the narration re-read -- symbols over line numbers, and two more wrong facts
- `68e9a97` docs: replace every figure and citation that was asserted rather than measured
- `0a21f35` perf: stop building two host-RAM temporaries the frame build never needed
- `8844000` fix: defer the shard-release lookup in _release_level_state

## The Remedy (NOT YET IMPLEMENTED — this is the whole point of the handoff)
| Where | Change |
|------|------|
| `src/et_miner/gpu/row_split.py:901-912` | `old = 12N + max(4N, 2R)`; `saving = max(4N−R, R) >= R > 0`; crossover k̄=4; DELETE the 1.67× ratio (correct form is `4N / max(4N−R, R)`, but it is not earning its line); re-scope the worked instance as the k̄=5 branch. Also `:904-906`'s "peak relocates to `widths`" holds only above the crossover. |
| new test | `tracemalloc` peak `< 1.05 * (8 * total_items + 8 * (total_rows + 1))` — **symbolically, never a literal**. All-domain `reset_peak()`/`get_traced_memory()` (no domain constant; non-NumPy share is 0.025%, which licenses both the band and the API choice — state that coupling). Catches `.astype` 1.351×, shifted-view cumsum 1.298×, `widths` 1.298×. NOT under `test_row_split_dtypes.py`'s issue-scoped docstring — widen it or use a sibling file. |
| `src/et_miner/gpu/kernels/loader.py:133-135` | No cardinal. Four clauses over four sets: all four entry points call this; the two single-GPU bodies pin their whole launch to home; the two multi-GPU wrappers pin per worker and pin their decode/merge tail to it; `build_prefix_groups_gpu` does not call this and **pins to its own input's device instead** (it DOES pin — `gpu_resident.py:39`). |
| `loader.py` | Add `_assert_rank` AND `_assert_dtype` beside `_assert_home`/`_assert_k_supported`; `_assert_dtype` RAISES per the module's stated rule at `:151-155`, does not cast. |
| `src/et_miner/gpu/kernels/gpu_resident.py` | Both called at **all four** entry points (`:126, :262, :355, :525`), ordered **home → rank → dtype → k-cap**; K=2's rank guard is `ndim != 1`. Reason absorbed into the existing comment at `:118-125` once, not copied four times. |
| `gpu_resident.py:251-252` | Move the family-wide claim to the **module docstring** at `:1-2`: "Every gpu-resident entry point enforces the same preconditions on its device inputs — co-residency, rank, dtype — differing only in the rank each requires (1-D `freq_cols_gpu` here, 2-D `prev_freq_gpu` at K>=3). The K>=3 twins carry one further guard, the shared-memory k-cap, which has no K=2 counterpart because K=2 is always k=2." Say **inputs**, not "device discipline" — pinning genuinely differs between the two kinds of wrapper. |
| `bench/runner.py` | `latest = {r["id"]: r for r in rows}`; `all_ids` from the **UNFILTERED** matrix; `missing`/`bad`/`stale` with `bad` and `stale` scoped to `all_ids`; `covered = not missing and not bad and not stale`. Deletes `fresh`, `replayed`, `failed`, `stale_rows`, `matrix_ids`, the duplicated filter and the empty-selection branch. Plus `return 1` on a failed row produced here; fix `:268`'s `next(...)` returning the FIRST row per id; print the matrix structure in the ✓ line. Leave `check_equivalence` UNSCOPED with a comment naming `('stress_k2', 2, None, False)`. |
| `bench/run_smoke.sh` / `run_full.sh` | Key the directory inside `main()` from `_git_rev()` (keeps `-dirty`); delete `CAMPAIGN_OUT` from both. **Both modes share one directory, deliberately.** |
| `bench/baseline/perf.md:3` and `:117` | Delete the "pinned at 22cb1dc + PR 0 / never re-recorded" premise, point at `:93-97`. Exemption stands. |

Ordering if anything is cut: **land the identity first.** A green test beside a wrong claim makes the wrong claim look checked.

## Key Files
| File | Role |
|------|------|
| `src/et_miner/gpu/row_split.py` | `:901-912` the blocking identity; `:737-755` is the OTHER session's work |
| `src/et_miner/gpu/kernels/loader.py` | `:133-135` the miscounted sentence; where `_assert_rank`/`_assert_dtype` go |
| `src/et_miner/gpu/kernels/gpu_resident.py` | four entry points at `:126, :262, :355, :525`; `:251-252` the false "identical" claim |
| `bench/runner.py` | `:233-241`, `:248-252`, `:263-270`, `:285`, `:315`, `:333` — three config-set views |
| `BUGS_FOUND.md` (untracked) | Section 4 added this session: N20, N21, N22 |
| `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-snappy-sifakis.md` | The 12-PR programme plan + defect→commit index |

## Extra Context
- **The durable finding across three councils**, worth more than any individual defect: *ask what a sentence quantifies over versus what it says it quantifies over.* Six for six on this PR's narration defects — and the fifth and sixth were found INSIDE the fixes for the fourth and fifth. Four reviewers independently converged on a wrong number twice (the fixture-bound identity, then "four"), because they had all asked the same question of it. **Convergence among independent reviewers is not itself a check.**
- The two ways an untested claim enters a review, from the panel's own self-audit: **relayed on a peer's evidence without probing it**, or **derived rather than measured with the harness one command away**.
- Deferred and agreed, NOT in scope: **PR 6c** (a `@same_device(home=, rank=, dtype=)` decorator over the ~9 unpinned kernel wrappers, sequenced AHEAD of PR 7 because the default K=2 path aborts off-device), and the upstream `output_dir=""` validation fix (reject a falsy non-None value at `core/apriori.py:330`).
- Also still open from PR 0a: the exact `--min-support` behind the published base214m figures, which should close before PR 7.
