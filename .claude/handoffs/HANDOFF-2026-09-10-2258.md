# Session Handoff: ET-Miner, 2026-09-10 22:58

**Branch:** `council-session-blockers` @ `543c29d` (no upstream)
**Tree:** 6 modified, 5 untracked, 0 stashes — **ALL of it is ANOTHER SESSION's live work, none of this session's**
**Focus:** PR 6. Implemented the third council's remedy in full, then ran the fourth `/council review PR6`, which returned **4 BLOCK / 0 APPROVE** on TEN new items.
**Reason:** End of session. Council closed, agents stopped, remediation not started.

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `543c29d`. The tree will NOT be clean and that is expected — see "Blockers". If HEAD moved, read the diff before trusting "Current State".

## Current State
- **Nothing in progress. No file is being edited.**
- The third council's nine-row remedy is IMPLEMENTED and green (`512c773`, `8e38366`, `3d1e431`, `543c29d`).
- The FOURTH council then ran and **blocked it**: 4 BLOCK / 0 APPROVE, three rounds, ten items. All four teammates confirmed final votes and were stopped with `TaskStop`.
- **Every one of the ten items is a claim asserting coverage or scope its subject does not have**, and every one was introduced by this session's own commits.
- Remediation has NOT begun. The full item list is below; this file is the working document.

## Next Steps
1. **Remediate the ten items** (table under "Key Findings"). Suggested order: `runner.py:383` (one line, unanimous), then the two `_assert_*`/docstring set-membership items (need the structural closure), then the memory-test narration cluster, then `row_split.py:973`.
2. Re-run the gate set after. **`bash bench/run_smoke.sh` proves less than it looks** — see Dead Ends.
3. A fifth council is the user's call; `/council` is `disable-model-invocation` and must be typed by them.
4. Still owed and deliberately skipped: a **`CHANGELOG.md` entry for PR 6** (the other session was writing that file).
5. Out of scope by agreement: PR 6c (`@same_device` decorator), `output_dir=""` validation at `core/apriori.py:330`, the base214m `--min-support` question from PR 0a.

## Session Instructions
- User chose **proceed, skip CHANGELOG** when offered wait / proceed-skipping / proceed-with.
- `/council` cannot be launched from a session; ask the user to run it.
- Never `git checkout <file>` here — it has silently discarded uncommitted work. Use a scratchpad copy.
- Another session is live in this tree; commit by staging only your own hunks.

## Decisions & Rationale
- **Extracted `_build_deferred_frame`** (`row_split.py:858`) to create a GPU-free test seam. Rejected re-implementing the block in the test: it would pin nothing. Cut needed zero re-indentation.
- **`all_supports` stays in the caller** so a measured peak is the block's own allocation.
- **New sibling test files per concern** rather than widening issue-scoped docstrings: `tests/test_row_split_memory.py`, `tests/test_kernel_input_guards.py`.
- **`check_equivalence` left UNSCOPED**, verified: `('stress_k2', 2, None, False)` is the ONLY group spanning both campaign modes.
- **Campaign dir keyed in Python** (`runner.py::_campaign_out`), imported by `report.py`; both shell scripts lost `CAMPAIGN_OUT`.
- **Stopped the four council agents** after all four confirmed final votes and began restating closed business. Findings are fully captured here.

## Dead Ends
- Do not test the memory identity by paraphrasing the old code: a paraphrase keeping the per-chunk `widths` list alive across the `offsets` allocation measures **3R** and contradicts the identity. Run the pre-fix code at `0a21f35^` itself.
- Do not write a tracemalloc test without warming pyarrow: first `LargeListArray.from_arrays` costs ~26 MB of one-time init. Unwarmed it fails at small N and PASSES at large N — flaky in the direction that looks like success.
- Do not quote exact byte counts for the tracemalloc excess. They are artifacts of the measurement context, not of the code (see Key Findings).
- Do not re-propose: `"four"` in `loader.py`; keying the campaign dir by mode; scoping `check_equivalence`. All three rejected by earlier councils.
- Do not treat `bash bench/run_smoke.sh` green as evidence for the runner changes: the run is all-fresh-at-one-rev, so `ee23ebd`'s staleness path never fires and `runner.py:383` is on the failure path a green run cannot reach.

## Key Findings

**The ten blocking items** (per-item support; `auditor` dissents where noted):

| # | Where | Defect | Blocks |
|---|---|---|---|
| 1 | `tests/test_kernel_input_guards.py:18-22`, `:133-137` | Claims to pin `mining.py:974`; never imports the module. Mutation: guard file stays 8/8 green, `tests/test_gpu_resident_e2e.py:29` is what catches it. Contradicts `loader.py:240-242`. | architect, alternatives, auditor |
| 2 | `bench/runner.py:383` | `bad`/`stale` not disjoint → prints "0 of 2" where truth is 1; can go negative. Gate at `:365` is sound. | **all four** |
| 3 | `tests/test_row_split_memory.py:127-131` | Credits a backstop to a test that never calls `_build_deferred_frame`. Real backstop is `:93`. | architect, alternatives, auditor |
| 4 | `tests/test_row_split_memory.py:29-34` | 1.250/1.167/1.333 stated unconditionally; they are the k=5 column of `1.5k̄/(k̄+1)`, `(k̄+2)/(k̄+1)`, `(k̄+3)/(k̄+1)`. | architect, math (**auditor dissents → flag**) |
| 5 | `src/et_miner/gpu/kernels/loader.py:133-137` | Self-contradicting inside one sentence. | architect |
| 6 | `src/et_miner/gpu/kernels/gpu_resident.py:4-8` | "Every gpu-resident entry point…" false for `build_prefix_groups_gpu`, exported at `kernels/__init__.py:63`, called from `dispatch.py:224`, guards nothing. | architect |
| 7 | `src/et_miner/gpu/row_split.py:877-884` | "three return paths"/"both `_empty_result()` calls above" — has two; both calls stayed in the caller at `:848`, `:852`. | architect, auditor |
| 8 | `src/et_miner/gpu/row_split.py:973` | `k̄ = 2.365, measured` — one occurrence tree-wide, no artifact. Replacement must NAME the lattice (smoke 2.3977 vs d63 5.48 straddle the threshold). | alternatives, math |
| 9 | `tests/test_row_split_memory.py:21-28` | Byte counts attributed to shape variation are measurement-context artifacts. | architect (instance six) |
| 10 | `bench/runner.py:129` | `f"{sha}-dirty"` is one string for every distinct tree at a commit, so the `:364` staleness compare cannot separate them; the `:377-379` tick is unactionable when dirty. Measured live: returns `543c29d-dirty`, porcelain hash `a38ad33dbf18` absent from the string. | finding (alternatives, measured by auditor) |

**Required structural closure for 5 and 6:** a module-level `_GUARDED_ENTRY_POINTS` tuple plus `_EXEMPT`, and a test asserting every name `kernels/__init__.py` re-exports from `gpu_resident` is in exactly one. Rewording has failed there four times.

**Item 3/4 remedy fork** — the MINIMUM sufficient fix is (a); (b) is preferred by three reviewers and required by none:
- **(a) keep `BAND = 1.05`**: restate `:29-34` as the three closed forms with crossovers 7/3, 19, 39; state the integer-k dependence against the `:62-64` "harness property, not a code property" disclaimer; rewrite `:21-28` to the invariance; correct or delete `:127-131`. If a production-shape case is added, its 0.8% margin MUST be stated beside it.
- **(b) replace the mechanism** (reads generically — `SLACK_B`, or alternatives' two-N differential `abs(e2-e1) < 4096`): the production-shape case then clears by 24-38x and needs no caveat.
- **Binding on either**: state that at k̄ ≤ 2 the excess is exactly zero so no peak-based bound distinguishes the forms; and the "closes the k=2 blind spot" justification must appear nowhere — it was falsified three ways.

**Measured facts, settled:**
- Identity `old = 12N + max(4N, 2R)` holds at ratio 1.0000 for k̄ = 1,2,3,4,5,8 against the real pre-fix code at `0a21f35^`; crossover exactly k̄=4.
- astype excess over the identity = `max(4N − R, 0)`, **identically zero at k̄ ≤ 2**. Crossovers exactly 7/3, 19, 39.
- Canonical `smoke` preset: 694 itemsets / 1,664 items, k̄ = **2.3977**, k-hist `{1:118, 2:290, 3:202, 4:63, 5:18, 6:3}`, astype ratio 1.0581 (exact: identity 18,872 B, peak 19,968 B).
- No in-tree lattice reaches the (2, 7/3] window where the mechanisms differ: smoke 2.3977, deep_k 5.2853, skewed_rows 4.3382, stress_k2 1.9794. No config overrides `min_support` (`runner.py:74-100`).
- The tracemalloc excess is **context-dependent, not shape-dependent**: 2426 inside a function frame at every shape; 2378 at module level, 12/12 fresh processes. Math measured 2,378×29 / 2,602×1 across processes. The *invariance* (excess does not scale with items or rows) reproduces hard; no exact byte count is a property of anything.
- `SLACK_B = 65_536` clears a spurious red by **25x**, not 250x (the excess must exceed the slack, not the jitter width).
- Installed toolchain: numpy **2.2.6**, pyarrow 25.0.1, polars **1.43.2**.

## Blockers & Pending Decisions
- **User decides** whether to remediate now, and whether to run a fifth council afterwards.
- **Another Claude session has live uncommitted work here**: `core/prune_observer.py` (new), `tests/test_prune_observer.py` (new), `core/apriori.py` (+46), `core/rules.py` (+52), `tests/test_rules.py` (+106), `CHANGELOG.md` (+42), and **`gpu/row_split.py` (+18)** at `:103` and `:743`. Leave it alone. The remaining `git diff` should be byte-identical to that list.
- Scratchpad harnesses will NOT survive the session: `/tmp/claude-0/-root-projects-ET-Miner/ee3ca1fb-8500-4cb7-a363-db20b8222cb7/scratchpad/` — `crossover2.py` (reproduces the identity), `k2check.py` (the k̄ window), `const2.py` (the context-dependent constant), `exitcode.py`, `guards.py`, `band.py`, `mkraw.py`.

## Test Status
All green at `543c29d`, before the council:
- `uv run pytest -q`: **681 passed, 6 skipped**
- `uv run pytest -q tests/test_tier_equivalence.py`: 10 passed
- `uv run ruff check src tests bench`: clean
- `uv run python bench/selfcheck.py`: READY, 19 kernels, 2 devices
- `uv run python bench/repro/run_all.py`: 13 live, 11 fixed, 0 broken
- `bash bench/run_smoke.sh`: exit 0, 6 fresh, ✓ at `3d1e431-dirty`; second run replayed 6 and still ticked. **Auditor's caveat: nil evidentiary value for the runner changes.**
- `perf --compare`: hash `same` 10/11 (the 11th the documented `twophase-deepk` exemption); worst regression beyond noise +0.0% (`deepk-cpu-sparse-j1` +5.4% at 3 repeats → +2.9% against an 18.6% spread at 7).

## What Was Done
- `row_split.py:944-996` identity corrected; `:858` `_build_deferred_frame` extracted; `TYPE_CHECKING` import added.
- `tests/test_row_split_memory.py` (new, 5 tests), mutation-verified: reverting the in-place cumsum turns 4 of 5 red.
- `loader.py:191` `_assert_rank`, `:221` `_assert_dtype`; `:133` sentence rewritten with no cardinal.
- `gpu_resident.py`: four guards at all four entry points (`:126, :262, :355, :525`), ordered home → rank → dtype → k-cap; family claim moved to the module docstring.
- `tests/test_kernel_input_guards.py` (new, 8 tests); `tests/test_gpu_device_affinity.py` CONTROL note de-staled.
- `bench/runner.py`: one config-set view, `latest` last-wins, `return 1` on a failure produced here, `_campaign_out()`; `report.py` imports it; `CAMPAIGN_OUT` deleted from both scripts.
- `bench/baseline/perf.md:3,:117` false premise removed.
- Ran the fourth council (3 rounds), relayed and independently verified every finding, stopped all four agents.

## Commits This Session
- `543c29d` docs: re-measure the identity on a second instrument, and fix a quantifier
- `3d1e431` fix: the campaign gate had three views of the config set and trusted none
- `8e38366` fix: guard the two input preconditions that co-residency never covered
- `512c773` fix: the memory identity was a fixture value stated as an identity

## Key Files
| File | Role |
|------|------|
| `src/et_miner/gpu/row_split.py` | `:858` `_build_deferred_frame`; `:877-884` item 7; `:944-996` identity; `:973` item 8; `:103`/`:743` are the OTHER session's |
| `tests/test_row_split_memory.py` | items 3, 4, 9 — the narration cluster |
| `tests/test_kernel_input_guards.py` | item 1 |
| `src/et_miner/gpu/kernels/loader.py` | item 5; `_assert_rank`/`_assert_dtype` at `:191`/`:221` |
| `src/et_miner/gpu/kernels/gpu_resident.py` | item 6 (module docstring) |
| `bench/runner.py` | items 2 and 10 |
| `BUGS_FOUND.md` (untracked) | Section 4: N20 (fixed), N21, N22 |

## Extra Context
- **The durable finding, now at ten-for-ten**: ask what a sentence quantifies over versus what it says it quantifies over. Every blocking item this council found is that shape, and every one was introduced by the commits that were fixing earlier instances of it.
- **The panel committed the same defect against itself at least seven times** and caught each against an artifact, never an argument: architect alleged a systematic briefing failure it could not verify and retracted ("I raised it to three; it was zero"); alternatives denied authoring its own round-1 vote because its working context began at round 2 and it read absence-from-context as absence-in-fact; math attached `numpy 1.26.3 / polars 1.44.1` to a measurement taken on numpy 2.2.6 / polars 1.43.2 — the same defect it blocked `row_split.py:973` over.
- **Convergence among independent reviewers is not a check; the artifact is.** Math and alternatives swapped sides on the band mid-council, each citing the other's superseded argument — the item would have closed as "nobody blocks it" had auditor not flagged the seat-swap.
- Orchestration note adopted for future councils: forward each vote's EVIDENCE, FLAGS and SUGGESTION lines **verbatim**, not condensed. A paraphrase of a line-cited finding is a derived artifact standing in for a primary source.
