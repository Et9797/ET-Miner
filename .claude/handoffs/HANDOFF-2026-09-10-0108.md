# Session Handoff: ET-Miner, 2026-09-10 01:08

**Branch:** `council-session-blockers` @ `e99fc14` (no upstream; branched off `04770ca`, chain off `main` @ `22cb1dc`)
**Tree:** 1 modified (`.gitignore`, pre-existing), 3 untracked (`BUGS_FOUND.md`, `applications/`, `runs/`), 0 stashes
**Focus:** Fix the council's blocker on `7ae1cb3`, then the agreed flag list
**Reason:** Task switch — review finished, remediation not started

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `e99fc14` and the tree should match "Tree" above. If not, the state moved after this handoff: read the diff before trusting "Current State".

## Current State
- **PR 6 is committed (7 commits) and then BLOCKED by a `/council review` of itself.** Nothing has been fixed yet; no work is in progress in any file.
- Verdict: **BLOCK on `7ae1cb3` alone** (auditor, architect, alternatives); math held APPROVE on lane grounds while verifying both defects and explicitly not contesting the block. The other six commits are unanimously approved.
- Last commands: `git diff 04770ca..e99fc14` symbol checks confirming (a) the k2 co-residency *contract sentence* is new in `7ae1cb3`, (b) `errorPeerAccessAlreadyEnabled` is in 32 of 62 CuPy `.so` files, so it is Cython boilerplate and proves nothing.
- Tree verified clean over the target set after reviewers edited and restored sources; both GPUs at 0 MiB.

## Next Steps
1. **Fix the blocker in `src/et_miner/gpu/kernels/gpu_resident.py`** (see Blockers for the agreed remedy). Amend `7ae1cb3` or add a follow-up commit — decide which, the chain is unpushed.
2. Flags, in the council's priority order: numpy floor; `d63` threshold (see the unresolved 2-to-1 split); `row_split.py:846` 16N→12N; fold `_free_sets` into Tier B; correct the #29 figures.
3. Smaller flags: `d63:202` env restore; three miscited line numbers in `row_split.py:830-842`; `k2.py:156` stale comment; `_release_level_state`'s eager second limb; drop the "no output change" label for #26.
4. Re-run the full gate set, then re-review (a fresh council, or `verifier`).
5. Only then PR 7 (#33 fast-track) — and close the base214m `--min-support` question first.

## Session Instructions
- `/council review PR6` was run in **review mode**: verdict only, no implementation. Nothing was to be changed, and nothing was.
- The user has not yet said which flags to take, nor who writes the fix (this session vs. a fresh one, since the council corrected this session's own work).

## Decisions & Rationale
- **Council stopped at round 2 of 3.** The panel converged completely on facts and remedy; the only split was procedural (math declining to vote outside its lane). Round 3 could not change the outcome. Deviation was stated, not hidden.
- **k2 *guard* is in scope, k2 *body* is not.** `count_pairs_fused_k2_gpu_resident` is byte-identical across `04770ca..e99fc14`, but `gpu_resident.py:271-272`'s contract sentence ("That device used to be hardcoded as 0") is new — so the commit wrote a promise into one wrapper and the check into the other. Verified by `git diff`.
- **`free_all_blocks()` kept on the borrowed branch in `#30`** — it is not scoped to this function's allocations, and `#31` adds the same call as material. Only the log line changed.
- **Flushed parquet stays `large_list<int32>`** — widening doubles itemset bytes on 51 GB of artifacts, and no reader is dtype-sensitive.
- **d63's oracle is exhaustive DFS, not a second apriori** — Defect A is a candidate-generation defect, so an oracle that also prefix-joins could share the blind spot.

## Dead Ends
- Do not use efficient-apriori as the oracle at d63's full scale: measured 13.5 s / 26.3 s / 47.9 s at 14 / 16 / 18 items on 20k rows, doubling per two items ⇒ ~10 h at 32 items / 120k rows.
- Do not fix the numpy floor by swapping to `np.unpackbits(...).sum()`: numerically exact but **21.8× slower** (4.67 → 102.06 µs/node), taking the DFS 5.4 s → 117.3 s and falsifying `d63:55`'s "does not need the `slow` marker". Bump the floor instead.
- Do not infer peer-access behaviour from symbol tables: `errorPeerAccessAlreadyEnabled` is in 32 of 62 CuPy modules including `_core/_dtype.so`. Three agents (math, architect, me) got this wrong in sequence. `_deviceEnsurePeerAccess` is in only `cuda/device.so` and `api/runtime.so`.
- Do not `git checkout <file>` to undo a control experiment — it discarded uncommitted work twice this session. Use `git stash push -- <paths>` or a scratchpad copy.
- Do not rest any argument on sm_90 "silent failure": retracted by math and dropped by architect. The block stands on hardware-independent grounds.

## Key Findings
- **The blocking regression**: `7ae1cb3` made `build_prefix_groups_gpu` device-following (`gpu_resident.py:29-33`), which removed the *incidental* raise that used to fire inside `_build_prefix_groups_gpu_impl` before the body could launch. `count_k3plus_gpu_resident`'s body (`:113-124`) is byte-identical and still allocates/launches on the **ambient** device. Pre-fix: `ValueError: The device where the array resides (1) is different from the current device (0)`. Post-fix: `CUDA_ERROR_ILLEGAL_ADDRESS`, which poisons the CUDA context process-wide — a lost campaign, not a lost level. Reverting only `:29-30` restores the ValueError.
  - Accurate phrasing for the commit message: *"the commit removed the thing that was catching it"*, and *"pre-fix, the only path that reaches a launch raised"* — the early returns at `:41-44`/`:47-52` read `prev_freq_gpu` through `.shape` only (host-side), never raised, and yield `total_candidates == 0` so no launch follows.
- **The guard is bypassable**: the co-residency `ValueError` sits at `:487-499`, *after* `if n_gpus <= 1: return ...` at `:463-464`. `n_gpus = min(n_gpus, available_gpus, total_candidates)`, so any single-GPU host and any `total_candidates == 1` level bypass it. `dispatch.py:230` — the caller the commit's own comment names as its reason — takes the unguarded branch. `test_mixed_device_inputs_raise_rather_than_being_repaired` passes only because it picks `n_gpus=2`.
- **This breaks a rule this programme wrote**: `core/apriori.py:523-527` puts `_validate_route_support` *above* the routing, with a comment saying placement below it "is how streaming came to swallow prune_equal_support" (PR 3's lesson).
- **Severity ceiling**: deterministic crash, **never a wrong count**. No P2P on this box (`P2P from 0: - 0`; no `enable_peer` anywhere in the tree), and `Stream.null.synchronize()` at `:145` raises before `n_results.get()` at `:147`. UVA rules out a foreign pointer silently resolving to unrelated data. **Nothing under `runs/` needs re-checking; base214m is not implicated.**
- **The remedy is count-neutral** (math, measured): same K=3 level on device 0 vs device 1 → `n=9880 sum=8,485,054` both, `np.array_equal` True.
- **numpy floor is broken for real**: `uv pip compile --resolution lowest-direct` gives py3.10→1.22.4, py3.11→1.23.2, py3.12/3.13→1.26.0. `np.bitwise_count` (used at `d63:144`) is NumPy ≥ 2.0 only, so **every** reachable resolution breaks the new gate. `pyproject.toml:44` declares `numpy>=1.20` (unreachable); `pandas>=2.3.3` does the lifting. Only `uv.lock` hides it — a plain `pip install .` does not have it.
- **`row_split.py:846` peak memory is 16N, not 12N** (math, VmHWM at N=200M: 0.77 GB baseline → 3.00 GB as written → 2.26 GB preallocated). Pre-fix was 8N, so `#26` doubles peak host RAM on the `output_dir=None` route.
- **My own #29 rationale figures are ~4× inflated** — "~35 s per level at 10M itemsets" measures to ~8.3 s; "371 B per itemset" to ~262 B at k=5. Taken from `BUGS_FOUND.md` unverified. They appear in **three** places: `row_split.py:511-517`, `:606-608`, `mining.py:274-279`.
- **Tier B validates the lattice, not `_free_sets`** — the generator layer, which the verdict actually consumes, sits outside the oracle's root of trust. Math verified it by hand once (5,868 == 5,868 vs all-subsets brute force); the committed gate does not.
- **`docs/` is gitignored** (`.gitignore:8`), so `docs/specs/et_miner_fix_spec.md` is **not tracked** — `BUGS_FOUND.md` and the plan both claim it is checked in. The amendment written this session lives only in the working tree, deliberately uncommitted.
- **N21 (filed, unfixed)**: with `CUDA_VISIBLE_DEVICES=0` all 121 tests in the seven files using `n_gpus=2` still pass, because PR 5's `_resolve_gpus` caps at `available`. So `test_multi_gpu_legacy_matches_oracle` and its two siblings pass on one GPU having exercised the single-GPU path.

## Blockers & Pending Decisions
- **The agreed remedy** (all three blocking reviewers converged):
  1. Add `_assert_home(bitvecs_gpu, **named)` to `gpu/kernels/loader.py` beside `_assert_k_supported` (already the first statement of both wrappers; needs no cupy import).
  2. Call it **above the routing** in both wrappers: `gpu_resident.py:452` (k3plus, after `n_freq, k_prev = prev_freq_gpu.shape`, above `build_prefix_groups_gpu` at `:455` and above the early return at `:463`) and `:290` (k2, after `n_freq = len(freq_cols_gpu)`, above the return at `:299`). Delete the loop at `:487-499`.
  3. Guard checks **two** arrays (`bitvecs_gpu`, `prev_freq_gpu`), not five — the three derived ones follow `prev_freq_gpu` by construction post-`:32`, and naming them points the error at the wrong array.
  4. Give `count_k3plus_gpu_resident` (`:101`) the wrapper+`_impl` split that `:29-33` established.
  5. Extend `tests/test_gpu_device_affinity.py:181-199`: parametrise `n_gpus` over `(1, 2)` — **fails today at 1** — and add both-inputs-on-device-1 with ambient 0 via `with cp.cuda.Device(0)`.
  - None of this is an output change for any in-tree caller (every route builds on device 0), so no existing gate or the tier chain is at risk.
- **UNRESOLVED 2-to-1, user's call**: how to fix `d63:175`/`:235`'s `math.ceil(MIN_SUPPORT * N_ROWS)`. architect + alternatives want inline `math.ceil(Fraction(str(MIN_SUPPORT)) * N_ROWS)` to keep the oracle independent of the code under test; math wants `_min_count` imported. All three agree the current form is wrong (`math.ceil(0.07*10000)` = 701 vs exact 700). If the inline form is taken it needs a call-site comment saying it is deliberately *not* the miner's helper, or `core/result.py:36-40`'s "three implementations must move together" makes the import look like a tidy-up.
- **User has not chosen** which flags to take, nor whether this session or a fresh one writes the fix.
- **Standing decision, still open from PR 0a**: the exact `--min-support` behind the published base214m figures. Should close **before** PR 7 (#33 moves supports on the multi-GPU path).
- **Standing defects, not PR 6**: the k2 *body* (`gpu_resident.py:189-262`, byte-identical, reproduces `cudaErrorIllegalAddress`); `mining.py:842`'s unprotected `sparse_state.release()` with no try/finally around the level loop at `:650`; `bench/repro/run_all.py` invoked by no `bench/*.sh` while `CHANGELOG.md:286` calls d63 "the standing gate".

## Test Status
All green at `e99fc14` and independently re-run by `auditor`:
- `uv run pytest -q`: **640 passed, 6 skipped** (241.92s). Baseline at `04770ca` was 607/4 on `-m "not slow"`; now 631/4.
- `uv run pytest -q tests/test_tier_equivalence.py`: **10 passed** (run on every commit)
- `uv run ruff check src tests bench`: clean
- `uv run python bench/selfcheck.py`: **READY**, 19 kernels on both 3090s
- `bash bench/run_smoke.sh`: exit 0, `equivalence groups consistent ✓`
- `uv run python bench/repro/run_all.py`: **13 live, 11 fixed, 0 broken**; `d63 FIXED devices=2 nodes=1,149,016 lattice=287,104 free=263,458 mined=263,458 missing=0 extra=0 cntdiff=0`
- `perf_baseline.py --compare`: worst regression beyond noise **+3.5%**; `itemset_hash` same on all 11 configs; peak VRAM flat or down (max −11.6%)
- **The green suite proved nothing**: two real defects sat behind all of it, in the failure path `7ae1cb3` exists to define.

## What Was Done
- Seven commits implementing the approved PR 6 plan (see below), each with a control verified to fail when the patch is absent.
- New tests: `tests/test_row_split_dtypes.py`, `tests/test_gpu_ownership.py`, `tests/test_gpu_device_affinity.py`, plus three classes appended to `tests/test_gpu_correctness.py`.
- New gate: `bench/repro/d63_spec_defect_a_row_split_drop.py` — two-tier oracle (exhaustive DFS at spec scale + efficient-apriori validating it at 16 items/20k rows).
- `multigpu` marker registered at `pyproject.toml:94-104`, applied **per test** and only to the affinity suite (measured: the seven other candidate files degrade gracefully to 1 GPU).
- `docs/specs/et_miner_fix_spec.md` amended in place (uncommitted — gitignored tree).
- `/council review PR6` run to completion: 2 rounds, 4 reviewers, verdict BLOCK on one commit.
- Two memories updated at `/root/.claude/projects/-root-projects-ET-Miner/memory/plan-review-standard.md`: review narration as an artefact; a positive grep is not a control.

## Commits This Session
- `e99fc14` docs: give spec Defect A a standing gate; correct the intrinsic list
- `7ae1cb3` fix: multi-GPU wrappers honour the caller's device (N10, N20) ← **BLOCKED**
- `a166956` fix: release the K>=3 group arrays on an aborted level (#31)
- `c0fa6f9` fix: never claim to free VRAM this route does not own (#30)
- `3096954` fix: build the apriori prune set only where it is read (#29)
- `ab935c0` fix: release the two level-scoped GPU allocations independently (#27)
- `17d8574` fix: one itemset dtype on every row-split return path (#26)

## Key Files
| File | Role |
|------|------|
| `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-floofy-dongarra.md` | The approved PR 6 plan, incl. the deferred PR 6b (retry helper) and its Step 0 |
| `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-snappy-sifakis.md` | The 12-PR programme plan + defect→commit index + ground rules |
| `src/et_miner/gpu/kernels/gpu_resident.py` | **The blocker lives here** — `:29-33`, `:101`, `:290`, `:452`, `:463-464`, `:487-499` |
| `src/et_miner/gpu/kernels/loader.py` | Where `_assert_home()` belongs, beside `_assert_k_supported` |
| `tests/test_gpu_device_affinity.py` | The affinity suite; `:181-199` is the test that needs `n_gpus` parametrised |
| `bench/repro/d63_spec_defect_a_row_split_drop.py` | Defect A's standing gate; `:144` numpy floor, `:175`/`:235` threshold, `:202` env leak |
| `BUGS_FOUND.md` (untracked) | The 62 defects; amend at the end with the new findings (N20, N21) |

## Extra Context
- **`fix the blocker`** is the stated next task. Everything needed is in "Blockers & Pending Decisions" — the remedy is ~10 lines plus a `parametrize`, and the panel already settled placement, array count and helper location.
- **The council corrected this session's own work repeatedly.** The single most useful finding, from `alternatives`: five separate defects across four lenses were one defect — inflated #29 figures, a "no output change" label over a real schema change, three wrong line citations, "costs nothing" over a measured doubled peak, and a d63 header overstating Tier B's coverage. *The narration was not reviewed to the standard the code was*, in the PR whose stated product is that comments match behaviour. Treat a narration re-read of all six commits as a condition of re-submission, not a follow-up.
- The in-tree standard to copy already exists: `tests/test_row_split_dtypes.py:133-156` states outright what its own scan cannot see.
- Reviewer probe scripts are in the session scratchpad `/tmp/claude-0/-root-projects-ET-Miner/292e4aea-0a5c-4bbd-b214-fc405bab8f20/scratchpad/` — they will not survive.
- Council teammate names (`architect`, `math`, `alternatives`, `auditor`) are one-shot and in-process; they do **not** survive `/resume`. Start a fresh council rather than messaging them.
