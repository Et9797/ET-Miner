# Session Handoff: ET-Miner, 2026-09-10 21:57

**Branch:** `council-session-blockers` @ `543c29d` (no upstream)
**Tree:** 6 modified, 5 untracked, 0 stashes — **ALL of it is ANOTHER SESSION's live work, none of this session's**
**Focus:** PR 6 remediation. The third council's 4-BLOCK remedy is now IMPLEMENTED in full.
**Reason:** Work complete; user asked to regenerate the handoff before running the fourth council.

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `543c29d`. The tree will NOT be clean and that is expected — see "Blockers". If HEAD moved, read the diff before trusting "Current State".

## Current State
- **The remedy is done.** All nine rows of the previous handoff's remedy table landed in four commits (`512c773`, `8e38366`, `3d1e431`, `543c29d`). Nothing is in progress.
- Full gate set re-run and green at this tree (see Test Status).
- **Next action is the fourth `/council review PR6`, which the USER must type** — `/council` is `disable-model-invocation` and cannot be launched from a session.
- The previous handoff (`HANDOFF-2026-09-10-2025.md`) is superseded; its remedy table is marked "NOT YET IMPLEMENTED" and is now false.

## Next Steps
1. **User runs `/council review PR6`** on `e70225e..543c29d`.
2. Relay and verify its findings; do not accept a peer's evidence unprobed (see Extra Context).
3. Still owed: a **`CHANGELOG.md` entry for PR 6**. Deliberately skipped — the other session was writing that file 2 min before this one started.
4. Deferred, agreed, NOT in scope: **PR 6c** (a `@same_device(home=, rank=, dtype=)` decorator over the ~9 unpinned kernel wrappers, sequenced AHEAD of PR 7); the upstream `output_dir=""` validation fix (reject a falsy non-None value at `core/apriori.py:330`); and from PR 0a, the exact `--min-support` behind the published base214m figures, which should close before PR 7.

## Session Instructions
- User was asked wait / proceed-skipping-CHANGELOG / proceed-with-CHANGELOG and chose **proceed, skip CHANGELOG**. That is the only reason CHANGELOG.md is untouched.
- `/council` cannot be launched from a session; ask the user to run it.
- Never `git checkout <file>` to undo an experiment in this repo — it has silently discarded uncommitted work. Use a scratchpad copy.
- Another session is live in this tree; commit by staging only your own hunks (see Blockers).

## Decisions & Rationale
- **Extracted `_build_deferred_frame` from `_apriori_row_split_multi_gpu`** (`row_split.py:852`). The remedy demanded a `tracemalloc` test, but the block was GPU-only and unreachable. Rejected re-implementing the block in the test: it would have pinned nothing, which is the exact failure the PR is about. The cut needed **zero re-indentation** — the code was already at 4-space depth.
- **`all_supports` stays in the CALLER**, passed in. A peak measured across the call is then the block's own allocation, which is what makes the band `8*total_items + 8*(total_rows+1)` exact.
- **`_assert_rank`/`_assert_dtype` take `name=(array, expected)` tuples**, not one expected value per call: K=2 needs a 2-D `bitvecs_gpu` beside a 1-D `freq_cols_gpu` in one call.
- **`_assert_dtype` raises rather than casting**, per the rule already stated on `_assert_home`; a hidden `.astype` would also silently double the array where the K>=3 path is most VRAM-bound.
- **`check_equivalence` left UNSCOPED** with a comment naming `('stress_k2', 2, None, False)`. Verified: it is the ONLY group spanning both modes, holding exactly `smoke:stressk2ml2-{legacy,shared}-2g` + `full:stressk2-filter-{compact,cupy,cpu}`.
- **Campaign dir keyed inside Python** (`runner.py::_campaign_out`), imported by `report.py`, rather than in the two shell scripts.
- **A new sibling test file per concern**, not widened docstrings: `tests/test_row_split_memory.py` and `tests/test_kernel_input_guards.py`.

## Dead Ends
- Do not test the memory identity by paraphrasing the old code. A paraphrase that keeps the per-chunk `widths` list alive across the `offsets` allocation measures **3R** and contradicts the identity. The real `np.concatenate([...])` releases the list on return, giving 2R. Run the pre-fix code itself (`0a21f35^`).
- Do not write the `tracemalloc` test without warming pyarrow. The first `LargeListArray.from_arrays` in a process costs ~26 MB of one-time init that tracemalloc bills to the first traced region. Unwarmed the test fails at small N and PASSES at large N as the constant amortises away — flaky in the direction that looks like success.
- Do not state the non-NumPy share as a percentage. Measured across five shapes it is a shape-independent **constant of ~2,378 B**; a percentage makes it a property of the fixture again.
- Do not fix `loader.py`'s miscounted sentence by writing "four" (four reviewers converged on it and were all wrong) — see the previous handoff. It now carries no cardinal at all.
- Do not trust the previous handoff's `row_split.py` line numbers: they were **+18 stale**.
- Do not pin the row_split peak with VmHWM (stops being TRUE below N≈10M) or with a `np` namespace recorder (`.copy()` defeats it with a byte-exact correct total).

## Key Findings
- **The identity holds exactly.** `old = 12N + max(4N, 2R)` measured by tracemalloc against `0a21f35^` at **ratio 1.0000** for k̄ = 1, 2, 3, 4, 5, 8, crossover exactly at k̄=4. Agrees with the council's VmHWM figures once the 4N sources are counted on both sides: k̄=1 is 28N/16N = 1.75 vs 5.238/2.980 = 1.758.
- **The dtype defect is worse than recorded.** With `_assert_dtype` removed, an int64 `prev_freq_gpu` returns `[[0, 0, 0]]` **at count 8** where the correct answer is `[[0, 1, 2]]` **at count 8**. The COUNT IS IDENTICAL — a count-only check passes it. Measured on-device.
- **The previous handoff's `row_split.py` coordinates were +18 lines stale**, from the other session's `@@ -99,0 +100 @@` and `@@ -738,0 +740,17 @@`. The identity block was at `:919-931`, not `:901-912`.
- **`bench/README.md:32`'s "re-run smoke if needed" now ticks.** Verified end-to-end: a second `runner.py --mode smoke` replayed all 6 rows and emitted ✓ at exit 0.
- **`deepk-cpu-sparse-j1` flagged +5.4% against a 0.8% spread at 3 repeats**; at 7 repeats it is +2.9% against an **18.6%** spread — worst regression beyond measured noise +0.0%. Same pattern as the council's `smoke-cpu-polars` case.
- **`twophase-deepk` exemption re-verified independently**: `git merge-base --is-ancestor 865286c 2eb81b0` is true and `perf-baseline.json` holds `n_itemsets = 10752`. Only the premise at `perf.md:3`/`:117` was wrong; the conclusion stands.
- **One instance of this PR's own defect was introduced and caught by re-reading**, not by a test: a retained comment in `runner.py` still said the tick covers "every config in this invocation" after the predicate had been changed to quantify over the matrix. Fixed in `543c29d`.

## Blockers & Pending Decisions
- **`/council review PR6` must be typed by the user.** Nothing else blocks.
- **Another Claude session has live uncommitted work in this repo**: `core/prune_observer.py` (new), `tests/test_prune_observer.py` (new), `core/apriori.py` (+46), `core/rules.py` (+52), `tests/test_rules.py` (+106), `CHANGELOG.md` (+42), and **`gpu/row_split.py` (+18)** at `:103` and `:743`. Leave it alone; do not revert or stash it. The remaining `git diff` should be byte-identical to that list — it was at handoff time.
- Reviewer harnesses are in this session's scratchpad `/tmp/claude-0/-root-projects-ET-Miner/ee3ca1fb-8500-4cb7-a363-db20b8222cb7/scratchpad/` (`band.py`, `crossover2.py`, `share2.py`, `guards.py`, `exitcode.py`, `mkraw.py`). **They will not survive the session** — copy them out if wanted. `crossover2.py` is the one that reproduces the identity.

## Test Status
All green at `543c29d`:
- `uv run pytest -q`: **681 passed, 6 skipped** (was 650/6 at `e70225e`; +8 mine, +23 the other session's)
- `uv run pytest -q tests/test_tier_equivalence.py`: 10 passed
- `uv run ruff check src tests bench`: clean
- `uv run python bench/selfcheck.py`: READY, 19 kernels, 2 devices
- `uv run python bench/repro/run_all.py`: 13 live, 11 fixed, 0 broken
- `bash bench/run_smoke.sh`: exit 0, 6 run fresh, ✓ at `3d1e431-dirty`; second run replayed 6 and still ticked
- `perf_baseline.py --compare`: hash `same` 10/11, the 11th the documented `twophase-deepk` exemption; worst regression beyond noise +0.0%

## What Was Done
- `row_split.py:944-996` — identity replaced with `12N + max(4N, 2R)`, `saving = max(4N-R, R) >= R > 0`, crossover k̄=4, 1.67× ratio deleted, worked instance re-scoped to the k̄≥4 branch, second-instrument measurement added.
- `row_split.py:852` — `_build_deferred_frame` extracted; `TYPE_CHECKING` import added for `np`.
- `tests/test_row_split_memory.py` (new, 5 tests) — symbolic band, plus `test_band_rejects_the_known_regressions` keeping the band's discrimination checked. Mutation-verified: reverting the in-place cumsum in the real function turns 4 of 5 red.
- `loader.py:191,221` — `_assert_rank` and `_assert_dtype`; `:133` sentence rewritten with no cardinal.
- `gpu_resident.py` — four guards at all four entry points (`:126, :262, :355, :525`), ordered home → rank → dtype → k-cap; family claim moved to the module docstring; inline `ndim != 2` blocks and their duplicated comment deleted.
- `tests/test_kernel_input_guards.py` (new, 8 tests); `tests/test_gpu_device_affinity.py` CONTROL note de-staled.
- `bench/runner.py` — one config-set view; `latest` last-wins; `missing`/`bad`/`stale`; `return 1` on a failure produced here; ✓ line prints matrix structure; `_campaign_out()` added.
- `bench/report.py` — imports `_campaign_out`; `bench/run_{smoke,full}.sh` — `CAMPAIGN_OUT` deleted.
- `bench/baseline/perf.md:3,:117` — false premise removed in both places.

## Commits This Session
- `543c29d` docs: re-measure the identity on a second instrument, and fix a quantifier
- `3d1e431` fix: the campaign gate had three views of the config set and trusted none
- `8e38366` fix: guard the two input preconditions that co-residency never covered
- `512c773` fix: the memory identity was a fixture value stated as an identity

## Key Files
| File | Role |
|------|------|
| `src/et_miner/gpu/row_split.py` | `:944-996` the identity; `:852` `_build_deferred_frame`; `:103`/`:743` are the OTHER session's |
| `src/et_miner/gpu/kernels/loader.py` | `:122` `_assert_home`, `:191` `_assert_rank`, `:221` `_assert_dtype` |
| `src/et_miner/gpu/kernels/gpu_resident.py` | module docstring carries the family claim; four entry points |
| `bench/runner.py` | `_campaign_out()`; the coverage predicate in `main()` |
| `tests/test_row_split_memory.py`, `tests/test_kernel_input_guards.py` | the two new pins |
| `BUGS_FOUND.md` (untracked) | Section 4: N20 (fixed), N21, N22 |
| `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-snappy-sifakis.md` | the 12-PR programme plan + defect→commit index |

## Extra Context
- **The durable finding across three councils**, worth more than any individual defect: *ask what a sentence quantifies over versus what it says it quantifies over.* This session went seven-for-seven on it, and the seventh was introduced by the fix for the sixth. **Convergence among independent reviewers is not itself a check.**
- The two ways an untested claim enters a review: **relayed on a peer's evidence without probing it**, or **derived rather than measured with the harness one command away**. Both were hit this session and both were caught by re-measuring.
- New memory written: `concurrent-session-git-hygiene.md` — how to commit alongside another live session (stage per-hunk, never `git checkout <file>`, expect stale handoff line numbers).
