# Session Handoff: ET-Miner, 2026-09-09 22:18

**Branch:** `council-session-blockers` @ `04770ca` (no upstream; branched off `2eb81b0`, itself off `main` @ `22cb1dc`)
**Tree:** 1 modified (`.gitignore`, pre-existing), 3 untracked (`BUGS_FOUND.md`, `applications/`, `runs/`), 0 stashes
**Focus:** Fix all 62 defects in `BUGS_FOUND.md`, HIGH severity first — 27 done, 35 remain
**Reason:** End of session

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `04770ca` and the tree should match "Tree" above. If not, the state moved after this handoff: read the diff before trusting "Current State".

## Current State
- **8 commits landed this session.** PR 0 (baselines) + PRs 1–5 of the 12-PR plan, then two commits fixing defects a `/council` review found *in those fixes*.
- **27 of 62 defects closed** (#1–#28 minus the unfixed, plus new finding N1). 35 remain: PRs 6–11 in the plan.
- The plan is at `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-snappy-sifakis.md` — approved, still current, 12 PRs with a defect→commit index at the end.
- Last action: committed `04770ca` after a control run (relaxing `w[1] <= w[0]` → `w[1] < w[0]` in `rust_ext/src/lib.rs` turns 2 of 4 new tests red, then restored + rebuilt).
- **The Rust extension is built and current at `04770ca`.** Any revert of `rust_ext/` needs `cd rust_ext && uv run maturin develop --release` (~60 s).

## Next Steps
1. **Decide the fate of `council-session-blockers`** — merge into a PR chain or rebase. Nothing is pushed; no PRs opened this session.
2. **PR 6** (plan): #26, #27, #29, #30, #31, **N10**, plus docs (amend `docs/specs/et_miner_fix_spec.md` in place — all 3 of its defects are fixed at HEAD; add `__ffsll` + 32-bit `__popc` to CLAUDE.md's intrinsic list). **N10 must land before PR 10.**
3. **PR 7** (#33, #34, #35, #42, #47, #48, #49) — fast-track: #33 returns wrong supports on the default multi-GPU path, still live. Seen in this session's own logs: `Chunk 1 on GPU 1 failed: The device where the array resides (0) is different from the current device (1).`
4. PRs 8–11: IO/flush+GCS, config/CLI/_env, async_pipeline, ramdisk.
5. **Council leftovers, not yet done** (deferred deliberately, see Blockers).

## Session Instructions
- `/council review all PRs from this session` was run in **review mode** (verdict only). User then chose **"Fix the blockers, defer the rest"** — that scope is what `7f6bdda`/`04770ca` implement.
- Council target set was **source + tests (29 files)**, user-chosen over the recommended source-only.
- Earlier decisions (from the plan approval): fix `async_pipeline.py`/`ramdisk.py` **in place**, not delete; `mine_two_phase` **re-document**, not redesign; **three PRs per review section** → became 12 PRs; fan-out kernels **fix + route logging**.

## Decisions & Rationale
- **Padding column names (`core/matrix.py:136`) over sorting at emission**: fixes #1 at the root; `_build_result_df` is NOT the shared emission point its docstring claimed (#20) and would miss row-split, the one route already correct.
- **Three `min_count` implementations bound by one JSON fixture**, not one implementation: `apriori_from_csr` is public API and Tier 2 of the mandated chain. Fixture is `tests/fixtures/min_count_cases.json`, read by `tests/test_min_count.py` **and** a Rust `#[test]` (`serde_json` is a dev-dependency only).
- **float64 in `_sparse_matmul`**, not bypassing MKL for ints: +1.8% on the matmul-bound config vs forfeiting MKL entirely.
- **Refuse rather than forward** `anchor_items` on the bitvecs route: that call already forwards `output_dir`/`resume_from_k`, so forwarding anchors would reproduce the 838→57 loss on a second route.
- **Guard the reader, not the writer**, for anchored resume: `mine_two_phase` sets `output_dir` unconditionally, so guarding the write would raise on every invocation of the feature.
- **`min` + column rename** `max_k_minus1_support` → `min_k_minus1_support` (#12): a column named `max_` holding a min is worse than either.

## Dead Ends
- **Do not "fix" #22 by hoist+remap**: implemented for real in a prior session, measured to lose 129 of 321 — it repairs only the level below the first filtered one.
- **Do not flush the complete level and anchor only the in-memory return**: `gpu/row_split.py:738-742` returns `_empty_result()` whenever `output_dir` is set and `mine_two_phase` always sets it, so `anchor_items` would produce nothing.
- **Do not reject `output_dir` + `anchor_items`**: would raise on every `mine_two_phase` call (`row_split.py:903-919` sets `phase2_dir` unconditionally).
- **Do not wire `output_dir` into `_apriori_from_bitvecs` as a fix for the error messages**: a flush without a resume story is half a promise; rewording was taken instead.
- **Do not use `pytest.raises(Exception)` on pre-fix FFI behaviour**: `PanicException.__mro__ == ['PanicException', 'BaseException', 'object']` — such a test passes vacuously.
- **Do not `pkill -f perf_baseline`**: it matched this session's own shell and killed the foreground chain (exit 144). Kill by PID.
- **Do not trust sub-second perf configs**: `smoke-gpu2` has a 48.7% run-to-run spread and reported a phantom "+47.4% regression".
- **Do not `git add -A`**: `runs/` is 51 GB and `applications/` 398 MB, both untracked and not gitignored.

## Key Findings
- **The suite could not see most of this.** Reverting `rules.py:577` `.min()`→`.max()` left the full suite green at 584 passed — an output-changing fix with nothing pinning it. Now covered by `tests/test_self_sufficiency.py`.
- **`find_frequent_1` counts CSC column-list entries, not distinct rows** (`rust_ext/src/core/apriori.rs:217`,`:230`): `apriori_from_csr([0,3],[0,0,1],n_rows=1)` returned `(0,): 2` — **support 2.0**. Now rejected at the boundary. An anti-monotonicity check would NOT catch it (inflating K=1 moves supersets *further* below subsets); the symptom predicate is `count > n_rows`, not `>=`.
- **`count_itemsets_sparse_raw` binary-searches unsorted rows**: a descending row gave `sparse=2` vs `simd=3` (truth 3), a 33% undercount, selected by `hasattr(rust, "count_itemsets_simd")` at `core/sparse.py:553`. Tier 2 disagreeing with itself by build.
- **Three of my own test fixtures built malformed CSRs** and passed (`test_rust_ffi_boundary` strided view → rows of `[0,0]`; `test_sparse_mkl` unsorted `rng.choice` rows). Fixed the fixtures, not the guard.
- **base214m impact assessment (PR 0a)**: miner row count **76,890,945**; **0 of 189,999** thresholds shift under the #11 fix. #35 cannot fire there — the divergent items occupy the same rank at 0.001 and sort above all shared items at 0.002. Full detail in `bench/baseline/behaviour-change-impact.md`.
- **#33 confirmed live** in this session's logs (see Next Steps 3).

## Blockers & Pending Decisions
- **Council-deferred, explicitly not done** (all three reviewers agreed they are follow-ups, not conditions):
  - `_rerun_at_exact_capacity` retry helper in `gpu/kernels/loader.py`, modelled on `gpu/kernels/shared_tiled.py:159-194`. **`alternatives`' BLOCK is unaddressed by what landed** — all five kernels already do unconditional `atomicAdd` with a guarded write, so `n_actual` is the exact capacity and the retry is free.
  - `_resolve_route() -> Route` + a **three-valued** capability table (honoured / refused / **forwarded-but-degraded**). A boolean table would record `resume_from_k` on an anchored run as supported and rebuild the 838→57. `alternatives`' warning: *"three more hand-written conditions leaves the fourth combination waiting"* — three were landed.
  - Sentinel defaults for `max_ram_gb`/`max_vram_gb` (`core/apriori.py:371-372` are `800.0`/`70.0`, so "the caller set it" is unobservable; they are still silently dropped on row-split, gpu-resident and CPU).
  - Parquet key-value metadata (`level_complete`/`gate`/`n_transactions`) at `io/flush.py`, so resume can check rather than assume. `alternatives` verified polars drops it on the GCS path.
- **User decision still open from PR 0a**: the exact `--min-support` used for the published base214m figures. Cleared at 0.0005/0.001/0.002 only; re-check with `uv run python bench/baseline/min_count_impact.py --exhaustive --n-rows <N>`.
- **`mine_two_phase` default changed** `phase2_support` 0.00001 → 0.0005 (user-visible; in CHANGELOG).

## Test Status
`uv run pytest -q -m "not slow"`: **607 passed, 4 skipped, 11 deselected** at 22:15 (baseline at `22cb1dc` was 471/4).
`uv run pytest -q tests/test_tier_equivalence.py`: **10 passed**. `cd rust_ext && cargo test --lib`: **53 passed**. `uv run python bench/selfcheck.py`: **READY** on both 3090s. `uv run ruff check src tests bench`: clean.
Perf (`bench/baseline/perf_baseline.py --compare`): worst regression beyond measured noise **+3.4%**; `stressk2-cpu-sparse` **+0.0%**; every `itemset_hash` unchanged except `twophase-deepk` 10752→10570, accounted for exactly (294 frequent items at phase2_support − 112 anchors = 182).
**Exactly one pre-existing assertion changed all session**: `tests/test_streaming_bug.py` (it pinned #13's undercount as correct).

## What Was Done
- `src/et_miner/core/matrix.py:136` zero-padded column names; `:357` batch-minimum length filter.
- `src/et_miner/core/result.py:50`, `synthetic.py:81`, `rust_ext/src/core/apriori.rs:32` — `exact_min_count`, one shared fixture.
- `src/et_miner/core/sparse.py:215` float64; `:34-56` MKL glob probe; `:93-98` no import-time thread config; `:455` `n_jobs` via `rust_ext/src/core/utils.rs::with_thread_budget` (cached pool).
- `rust_ext/Cargo.toml` — `panic = "abort"` removed; `rust_ext/src/lib.rs` `contiguous()` + four-clause `validate_csr`.
- `src/et_miner/core/apriori.py` — `_validate_route_support()` above the routing; `core/rules.py:577` `.min()`; `:548` guarded append.
- `src/et_miner/gpu/row_split.py:683`,`:362` anchor emit-only; `_apply_anchor_filter` deleted from `gpu/mining.py`.
- `src/et_miner/gpu/kernels/loader.py` — `_warn_result_truncation` always raises; `MAX_SUPPORTED_K = 62` + `_assert_k_supported`.
- `src/et_miner/gpu/dispatch.py` — `_resolve_gpus()` with route logging on all five entry points.
- New tests: `test_emission_order.py`, `test_min_count.py`, `test_routing_contracts.py`, `test_rust_ffi_boundary.py`, `test_sparse_mkl.py`, `test_gpu_correctness.py`, `test_self_sufficiency.py`.
- New harness: `bench/baseline/` (impact assessment, min-count sweep, perf baseline with 3 repeats), `bench/repro/` (16 defect reproductions + `run_all.py`), `CHANGELOG.md`, version 0.2.0.

## Commits This Session
- `04770ca` fix: pin why the CSR row check is STRICTLY increasing, not just sorted
- `7f6bdda` fix: eight defects the council found in this session's own remediation
- `2eb81b0` fix: GPU correctness -- anchors, truncation, K cap, memory guard, routing
- `2090cdc` fix: self-sufficiency metric, batch length filter, and two wrong docstrings
- `18da0a8` fix: reject or forward every dropped routing parameter (#6-#10, N1)
- `865286c` fix: sparse numerics and the Rust FFI boundary (#4, #5, #16, #17, #18)
- `7b06872` bench(baseline): repeat each perf config and gate on measured noise
- `702828d` fix: exact decimal min-count at all three sites, one shared table (#11, #14)
- `98e8cf4` fix: emit itemsets in ascending item-id order on every route (#1, #2, #3, #20)
- `5a1fac0` bench: performance baseline, reproduction harness, CHANGELOG, 0.2.0

## Key Files
| File | Role |
|------|------|
| `/root/.claude/plans/handoff-saved-claude-handoffs-handoff-20-snappy-sifakis.md` | The approved 12-PR plan + defect→commit index + ground rules |
| `BUGS_FOUND.md` (untracked) | The 62 defects. Amend at the end with the 19 new findings (register in the plan) |
| `bench/repro/run_all.py` | `--expect-live` gates a repro before its fix; 16 scripts, `d<NN>_*.py` |
| `bench/baseline/behaviour-change-impact.md` | Which published numbers the remediation moves (base214m: none) |
| `bench/baseline/perf_baseline.py` | `--compare` for the per-PR delta; 3 repeats, gates on measured spread |
| `tests/fixtures/min_count_cases.json` | One table for the min-count rule, read by Python **and** Rust |
| `src/et_miner/core/apriori.py` | `_validate_route_support()` — three hand-written conditions; the refactor target |
| `rust_ext/src/lib.rs` | `contiguous()` + `validate_csr()`; the four-clause guard |

## Extra Context
- **Verification discipline that paid off, worth keeping**: every fix carries a control that fails when the fix is reverted. The council still found 8 defects the controls missed — because controls only test properties you thought to assert. What caught the rest was reviewers *reproducing each other* rather than accepting: `auditor` measured `architect`'s finding, `math` corrected `auditor` twice, `alternatives` withdrew four of its own proposals.
- **Corrections made to `BUGS_FOUND.md` itself** (record when amending): #58's `bounds[0]` is at `flush.py:227` not `:229`; `__popc` is in **2** `.cu` files not 10 (the report's grep matched `__popcll`); #36's "5,000 / 7 itemsets" should be 4,800 / 173; #55's fault is at `:109` only; the "seven defects in the dead modules" list names eight.
- Council teammate names are one-shot and in-process — they do **not** survive `/resume`. Start a fresh council rather than messaging `architect`/`math`/`alternatives`/`auditor`.
