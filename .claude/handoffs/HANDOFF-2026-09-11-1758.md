# Session Handoff: ET-Miner, 2026-09-11 17:58

**Branch:** `council-session-blockers` @ `b758632` (pushed, in sync with origin)
**Tree:** 6 modified, 5 untracked, 0 stashes — **ALL of it is ANOTHER SESSION's live work, none of this session's**
**Focus:** Remediated the 4th council's ten blockers, committed in 4 commits, opened PR #8, then ran the 5th council on those 4 commits.
**Reason:** End of session. Council closed 4 BLOCK / 0 APPROVE on NINE items; remediation not started.

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `b758632`. The tree will NOT be clean and that is expected — it is the other session's work (see Blockers). If HEAD moved, read the diff before trusting "Current State".

## Current State
- **Nothing in progress. No file is being edited. Nothing is uncommitted from this session.**
- The ten items from the 4th council are IMPLEMENTED and committed (`f5163d1`, `7d2b982`, `0ace189`, `b758632`), pushed, and **PR #8** is open: https://github.com/Et9797/ET-Miner/pull/8
- PR #8 carries all **39 commits** of the branch (first push), not just this session's 4. Stated in its description.
- The FIFTH council then reviewed only this session's 4 commits (`543c29d..HEAD`, 9 files) and **blocked**: 4 BLOCK / 0 APPROVE, three rounds, **nine items**. All four teammates stopped with `TaskStop`.
- **Every one of the nine is the same shape as the ten it was fixing**, and every one was introduced by these four commits.

## Next Steps
1. **Settle the one open dispute** (see Blockers): `SLACK_B` mechanism. Then implement the five units below.
2. **Unit A** → **B**; **C** → **D**; **E** anywhere. Order is forced by inert-check dependencies (a check must run before the claim it checks lands).
   - **A** Move the 7 device-free tests (`tests/test_kernel_input_guards.py:147-167` and `:201-348`) to a new device-free `tests/test_kernel_guard_claims.py`; remove BOTH gates (`:47` importorskip, `:59-60` module skip); fix the comment at `:195-198`; re-measure and restate the `:32-34` tally **in items** against both resulting files.
   - **B** In the new file, pin the full `file:lineno` at old `:348` (`"gpu/dispatch.py:224"`, not just the filename); replace `_call_sites`'s regex with `ast.walk` over `ast.Call`.
   - **C** The `"unknown"` triple, fails as a set: `covered = ... and here != "unknown"` at `bench/runner.py:430` with its own printed line; `stale` to treat a row rev of `"unknown"` as stale **unconditionally** (not by equality); `shutil.which("git") is None` at `tests/test_campaign_gate.py:127`; assert the sha is **hex** instead of `len(rev) >= 7` at `:140-142`; print the swallowed exception at `runner.py:156-157` so the sentinel is diagnosable.
   - **D** Reword `bench/runner.py:193-197` so the subject is the **invocation**; add post-loop `now = _git_rev()` drift check at `:372-429`.
   - **E** ONE commit on `tests/test_row_split_memory.py:16-47` (all edit one paragraph): N definition, `SLACK_B`, the `7/3` line, items 3 and 4 below, and the `:56-58` cause attribution.
3. Re-run the full gate set after (see Test Status for the commands and the baseline).
4. A sixth council is the user's call; `/council` is `disable-model-invocation` and must be typed by them.
5. Still owed and deliberately skipped: a **`CHANGELOG.md` entry** for this work (the other session owns that file).

## Session Instructions
- User said **"commit it and open a PR"** — done. No instruction to merge.
- `/council` cannot be launched from a session; ask the user to run it.
- Never `git checkout <file>` here — it has silently discarded uncommitted work. Use a scratchpad copy.
- Another session is live in this tree; commit by staging only your own hunks.

## Decisions & Rationale
- **Fork (b) for the memory bound** (absolute `SLACK_B`, not the 1.05 ratio band). Measured: the real implementation's excess is flat ~2.4 KB from 1,664 to 40,000,000 items while the ratio ranges 1.0000→1.1245, so the old band **rejects correct code** at the real smoke lattice. Fork (a) was therefore not viable as written.
- **Four commits, not one**, grouped by concern, matching the prior session's granularity.
- **`row_split.py` staged per-hunk** via `git apply --cached` on a filtered patch (`git add -p` is unavailable). Hunks `:103`/`:743` are the other session's and stayed unstaged.
- **Verified the staged tree stands alone** before committing: built a throwaway worktree from the index (`git write-tree` → `git commit-tree` → `git worktree add --detach`) and ran the full suite there — 692 passed, 6 skipped (692 = 710 − the other session's 18 tests).
- **Council scope pinned to this session's 4 commits** (9 files), not the whole 80-file PR: the other 35 commits each already passed an earlier council, and 80 > the skill's 25-file budget.
- **Forwarded each vote's EVIDENCE/FLAGS/SUGGESTION verbatim** in the round-2 digest, per the orchestration note adopted last session.

## Dead Ends
- Do not trust `bash bench/run_smoke.sh` exit 0 as evidence for the runner changes: an all-fresh-at-one-rev run never reaches the NOT-GATED branch. `tests/test_campaign_gate.py` is what covers it now.
- Do not edit any tracked file while a campaign is running: the rev digest changes mid-run, rows scatter across directories, and the results are contaminated. This happened this session (mutated `mining.py` during `run_smoke.sh`); the run was killed and `bench/results/campaign/543c29d-dirty.e2c1d6a347fe/` deleted.
- Do not run background Python without `-u` or `flush=True`: two `kbar` measurement jobs produced zero output for 10+ minutes purely from stdout buffering and had to be killed.
- Do not mine `stress_k2` on CPU to get its k̄ (2M rows @ min_support 1.5e-05) — it ran >10 min with no result. `deep_k`/`skewed_rows` on GPU take ~1 min.
- Do not test warm-order effects with an arm that never calls `_warm()`: that arm measures the ~26 MB unwarmed pyarrow init (25,592,388 B), not the effect. This produced a **wrong falsification** that had to be retracted (see Key Findings).
- Do not re-propose (rejected by earlier councils): a cardinal `"four"` in `loader.py` prose; keying the campaign dir by mode; scoping `check_equivalence`.

## Key Findings

**The nine blocking items from the 5th council** (all verified independently by the orchestrator, not taken on the vote):

| # | Where | Defect | Kind |
|---|---|---|---|
| 1 | `bench/runner.py:156-157`, `:288-291`, `:430` | `except Exception: return "unknown"` — `here` and every row's `rev` come from the same fn, so a git failure makes them **equal**, `covered` is True, and the gate prints `✓ (every row at unknown)` and exits 0. CLAUDE.md makes exit 0 the tier gate passing. | **only false green** |
| 2 | `tests/test_row_split_memory.py:79`, `:180` | `SLACK_B = 65_536` vs smoke identity 18,872 B = **3.47x the whole identity**; the row admits any peak ≤ 84,408 B. Measured: real 2,350 / astype 2,708 / cumsum **8,091** / widths **14,412** — ALL inside. | test checks nothing |
| 3 | `tests/test_row_split_memory.py` docstring of `test_the_other_two_regressions_are_still_caught_at_k2` | "they stay outside the slack at **every kbar**" — false at kbar 2.3977 by 8x and 4.5x. Quantifies over kbar; binding variable is N. Test only runs at 2M items so it cannot catch its own claim. | false + self-unverifiable |
| 4 | `tests/test_row_split_memory.py:43` | "the astype form is 1,332 B, **still below**" the real build — measured **2,676–2,708 B**, ABOVE real's 2,350 B, in both process orderings. Wrong number and wrong direction. (kbar ≤ 2 case still holds: 1,175 vs 2,378.) | false figure |
| 5 | `tests/test_kernel_input_guards.py:47`, `:49`, `:59-60` vs `:195-198` | Comment says the closure tests "run on a box with no device". Measured `CUDA_VISIBLE_DEVICES="" pytest -q` → **1 skipped**. **Seven** device-free tests hidden (incl. `:147-167`, item 5 of this very PR). Neither gate is needed: with cupy blocked via meta_path, `gpu.mining` and `kernels.gpu_resident` both import cleanly. | check doesn't run |
| 6 | `tests/test_row_split_memory.py:38-39` | `max(4N - R, 0)` stated "with **N = 8 * total_items**" while `row_split.py:951` defines **N = total items**. Fixture: predicts 121,599,992 B vs measured 9,601,628 B (**12.7x out**); makes "identically zero at kbar ≤ 2" false (would be ≈0.25). | wrong arithmetic |
| 7 | `tests/test_campaign_gate.py:127` | `subprocess.run(["git","--version"]).returncode` — raises `FileNotFoundError` **before** a returncode exists, so `pytest.skip("git not available")` cannot fire for git being unavailable; the 7 `tiny_repo` tests **error**. | guard can't fire |
| 8 | `bench/runner.py:193-197` | "editing the tree mid-campaign starts a new directory" — false of the RUNNING campaign: `out_dir` bound once at `:326`, per-config rev at `:235`/`:238`, `here` at `:372`. Gate output is still safe (rows land in `stale`, NOT-GATED). | wrong claim |
| 9 | `src/et_miner/gpu/kernels/gpu_resident.py:65` | hardcodes `gpu/dispatch.py:224`; `tests/test_kernel_input_guards.py:348` checks only the **filename**. Breaks the rule stated at `row_split.py:909-912` in the same PR. | unchecked |

**Also found, non-blocking:** `tests/test_kernel_input_guards.py:32-34` tally says "1 failed, 12 passed" (13 items); file now collects **15**; re-measured true value **"1 failed, 14 passed"**. `tests/test_campaign_gate.py:15-16` quotes `assert '13f486b-dirty' != '13f486b-dirty'` — `tiny_repo` sets no `GIT_AUTHOR_DATE` so the sha is time-dependent and unreproducible. `:31` says "98x-195x"; cumsum-copy is **97.66x** (lower bound rounded up). `_call_sites` regex is prose-falsifiable (a docstring containing `_assert_home(` turns the suite red) and its two sides count in different units (lines vs `str.count`) — all false REDS, so non-blocking; `ast.walk` fixes both. `row_split.py:983-991` kbar table has an in-tree artifact for only 1 of 3 rows (three lenses reported it).

**The 48-byte constant — settled, after I got it wrong.** `tests/test_row_split_memory.py:56-58` attributes the 2,426/2,378 spread to **call-frame depth**. That is false. The real variable is the order of `_warm()` relative to building the inputs. Measured, 4 fresh processes per cell:
```
smoke lattice   warm-before-build 2398 x4   build-before-warm 2350 x4
4M fixture      warm-before-build 2426 x4   build-before-warm 2378 x4
```
The fixture row reproduces the file's own pair exactly. I initially told `auditor` this cause was falsified — my test was broken (one arm never warmed; a follow-up conflated order with position) and I **retracted** it to both `auditor` and `math`. `math` withdrew "state no cause" in favour of citing the warm/build order; `auditor` argues for dropping the causal claim entirely. Unresolved between them.

**Verified correct and NOT to be re-litigated:** `_assert_home` has exactly 4 call sites, all guarded. `build_prefix_groups_gpu` has exactly 3 (`gpu_resident.py:214`, `:637`, `dispatch.py:224`), and `dispatch.py:224` calls it BEFORE the guard — a real gap, stated not fixed. "FOUR places return a frame" is right. The kbar table reproduces to the digit (smoke 2.3977 / skewed_rows 4.3382 / deep_k 5.2853, re-mined by two seats). The blake2b digest is 48 bits, collision ~1.8e-9 at 1,000 dirty trees. The `7/3` sweep returns exactly 3 hits repo-wide, **no fourth descendant** (`git log -S` puts `7/3` at `0ace189`, between `2.365`'s introduction at `512c773` and its removal at `b758632`).

## Blockers & Pending Decisions
- **USER DECISION — the one dispute the panel did not resolve.** `math`: `limit = min(SLACK_B, max(4096, identity // 8))` (unchanged at the fixture, 4,096 at smoke, recovers cumsum+widths, leaves astype correctly uncaught). `alternatives`: rejects it — the ratio term binds on **no** shape the file tests (smoke falls to the floor, the other four hit the cap, the middle term is live only for identities 32 KB–512 KB where no fixture sits), and the 4,096 floor is 1.69x the worst observed constant where `:59-60` demands 10x; derives **`SLACK_B = 16_384`** from the doubling threshold (a doubling's excess at the production shape IS the identity, 18,872 B), all existing assertions stay green. Third option, larger edit, `alternatives`' preference if paid for: fold smoke into `test_the_excess_does_not_scale_with_n` (`:199-221`), which **subtracts** the constant instead of tolerating it. The orchestrator's recommendation if asked to pick: `16_384`, because it is derived from the regression class the file exists to catch rather than fitted to the measurement.
- **Secondary, also unresolved:** `:56-58` — cite the warm/build order (`math`) or drop the causal claim entirely (`auditor`).
- **Another Claude session has live uncommitted work here**: `core/prune_observer.py` (new), `tests/test_prune_observer.py` (new), `core/apriori.py` (+46), `core/rules.py` (+52), `tests/test_rules.py` (+106), `CHANGELOG.md` (+42), `.gitignore` (+2), and **`gpu/row_split.py` (+18)** at `:103` and `:743`. Leave it alone. The remaining `git diff` should be byte-identical to that list.
- Scratchpad harnesses will NOT survive: `/tmp/claude-0/-root-projects-ET-Miner/008b7cf2-77bf-4c51-ad6d-9902a942c303/scratchpad/` — `slack.py`, `regr.py`, `inv.py`, `kbar2.py`, `warmorder.py`, `fresh2.py`, `signflip.py`, `kbarclaim.py`, `decisive.py`.

## Test Status
All green at `b758632`, measured this session:
- `uv run pytest -q`: **710 passed, 6 skipped** (was 681+6 at `543c29d`; +29)
- `uv run pytest -q tests/test_tier_equivalence.py`: 10 passed
- `uv run ruff check src tests bench`: clean
- `uv run python bench/selfcheck.py`: READY, 19 kernels, 2 devices
- `uv run python bench/repro/run_all.py`: 13 live, 11 fixed, 0 broken
- `bash bench/run_smoke.sh`: exit 0, 6 fresh, ✓ at `543c29d-dirty.6a715d305584` (**nil evidentiary value for the runner changes** — see Dead Ends)
- `uv run python bench/baseline/perf_baseline.py --compare bench/baseline/perf-baseline.json`: itemsets `same` 10/11 (11th the documented `twophase-deepk` exemption), worst regression beyond noise +0.0%

## What Was Done
- `bench/runner.py`: `_git_rev` now appends a 48-bit blake2b digest of `git diff HEAD`; `_coverage()` extracted as a pure seam at `:253-292`; `ok_here` by its own set membership; `_campaign_out` docstring updated; both shell scripts' rev comments updated.
- `tests/test_campaign_gate.py` (new, 13 tests), mutation-verified both directions.
- `gpu_resident.py`: `_GUARDED_ENTRY_POINTS` + `_EXEMPT_ENTRY_POINTS` at `:42-73`; module docstring quantifies over the tuples. `loader.py:130-145` names the tuple instead of "every".
- `tests/test_kernel_input_guards.py`: +7 tests (structural closure + 2 source-scan); `test_the_k1_seed_really_is_int32` split and rewritten to read `gpu.mining`'s source.
- `tests/test_row_split_memory.py`: ratio band → `SLACK_B`; +9 tests incl. the smoke-lattice case and `test_the_smoke_histogram_is_the_real_preset`.
- `row_split.py:877-884` return-path count corrected; `:983-1004` kbar table replaces the unsourced `2.365`.
- Pushed the branch and opened **PR #8**; ran the 5th council (3 rounds), verified every finding independently, corrected two reviewer claims with artifacts, retracted one of my own, stopped all four agents.

## Commits This Session
- `b758632` docs: count the set the sentence names, and name the lattice the figure came from
- `0ace189` fix: a ratio band measured the fixture; the excess it bounds is a constant
- `7d2b982` fix: the guard family was a sentence, and the sentence kept being wrong
- `f5163d1` fix: the gate's ok-count double-counted an overlap, and -dirty named every tree alike

## Key Files
| File | Role |
|------|------|
| `tests/test_row_split_memory.py` | items 2, 3, 4, 6 + the `:56-58` cause — unit E, ONE commit on `:16-47` |
| `tests/test_kernel_input_guards.py` | item 5 (units A/B) + the stale tally at `:32-34` |
| `bench/runner.py` | items 1 and 8 — `:156-157`, `:288-291`, `:430`, `:193-197` |
| `tests/test_campaign_gate.py` | item 7 at `:127`; vacuous `len(rev) >= 7` at `:140-142`; bad literal at `:15-16` |
| `src/et_miner/gpu/kernels/gpu_resident.py` | item 9 at `:65`; the two tuples at `:42-73` |
| `src/et_miner/gpu/row_split.py` | `:951` owns `N = total items`; `:983-1004` kbar table; `:103`/`:743` are the OTHER session's |
| `BUGS_FOUND.md` (untracked) | other session's |

## Extra Context
- **The durable finding, now nineteen-for-nineteen across five councils**: ask what a sentence quantifies over versus what it says it quantifies over. All ten items of the 4th council and all nine of the 5th are that shape, and every one of the nine was introduced by the commits fixing the ten.
- **Convergence between reviewers is not a check; the artifact is.** Two reviewer claims were falsified by measurement this session and both were withdrawn: `architect`'s "the tally matches no summary this file could have produced" (the tests landed four-then-two, so 13 was a real intermediate state), and `auditor`'s warm-order determinism claim — except that one turned out to be **correct** and my falsification was the broken thing. The orchestrator is not exempt from the standard.
- **`alternatives` declined to promote `_call_sites` to blocking** on a principled line worth keeping: all three of its failure modes are false REDS, and a test that can accuse an innocent author but cannot certify an untrue claim is a different severity from the items the council was convened on.
- **`math` converted an open-ended obligation into a bounded negative result** — ran the `7/3` descendant sweep rather than inheriting "a sweep is owed". Recording the negative result is what stops the next session from re-running it.
