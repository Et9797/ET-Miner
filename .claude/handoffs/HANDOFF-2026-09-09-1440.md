# Session Handoff: ET-Miner, 2026-09-09 14:40

**Branch:** `main` @ `22cb1dc` (in sync with origin/main)
**Tree:** 1 modified (`.gitignore`, pre-existing), 3 untracked (`BUGS_FOUND.md`, `applications/`, `runs/`), 0 stashes
**Focus:** Fix all 62 defects in `BUGS_FOUND.md`, HIGH severity first
**Reason:** Task switch — review complete, remediation not started

## Verify on Resume
Run `git status --short --branch` and `git rev-parse --short HEAD`. HEAD should be `22cb1dc` and the tree should match "Tree" above. If not, the state moved after this handoff: read the diff before trusting "Current State".

## Current State
- **The review is finished; no fixes have been started.** No source file under `src/`, `rust_ext/`, `tests/` or `bench/` was modified at any point this session — verified with `git status --porcelain`.
- Sole deliverable: **`BUGS_FOUND.md`** (untracked, 3,524 lines) — 62 confirmed defects, 17 HIGH / 26 MEDIUM / 19 LOW, produced by three sequential four-reviewer councils (host core + Rust; GPU + CUDA; streaming/IO/config/CLI), two rounds each.
- All 12 reviewer agents have stood down. Their scratch scripts are under `/tmp/mathstr_*.py`, `/tmp/auditorstr_*.py`, and `/tmp/claude-0/-root-projects-ET-Miner/1882ddcb-.../scratchpad/` — **`/tmp` will not survive a reboot**; the reproductions worth keeping are pasted inline in `BUGS_FOUND.md`.

## Next Steps
Fix in this order. The ordering is not arbitrary — see "Session Instructions" for the four constraints that make some orderings unsafe.

1. **#33** — multi-GPU streaming returns wrong supports. One line: thread the caller's device into `build_bitvecs_gpu_from_scipy` at `src/et_miner/gpu/bitvec.py:65`, matching the correct pattern at `gpu/csr_bitvec.py:512`. Then make the swallow impossible at `streaming/multi_gpu.py:370-371` and `:527-528`.
2. **#1/#2/#3/#35** — one root cause. Pad column names at `core/matrix.py:136` (`f"i_{idx:0{width}d}"`, `width = len(str(len(item_ids)))`); key `_build_support_lookup` by `tuple(sorted(...))` at `core/rules.py:43`; canonicalise at the SON **merge point** `streaming/son.py:298` + `streaming/multi_gpu.py:313`.
3. **#22** — `anchor_items`. Emit-only filter at `gpu/row_split.py:670-672`; leave `current_flat` and `full_flat` unfiltered; add the mask at the K=1 emit site `:357-360`; drop the `k < 2` clause at `gpu/mining.py:223`; **leave `n_freq` alone**.
4. **#4** — `core/sparse.py:118-121`: cast to `np.float64`, or bypass MKL for integer dtypes.
5. **#23/#24** — call `_warn_result_truncation` at `kernels/k3plus.py:447`, `kernels/gpu_resident.py:338`, `:491` (and `k3plus.py:193`, exported but unreachable); remove the ≤5% tolerance at `kernels/loader.py:32-40`.
6. **#6, #7, #8, #9, #10** — the dropped-parameter family at `core/apriori.py:436-473` and `:495-512`.
7. **#34, #36-#39** — remaining section-3 HIGHs (SON chunk swallow; async threshold; GCS object-name collision; low-disk shadowing; no completion marker).
8. Then MEDIUM, then LOW.
9. **Amend `docs/specs/et_miner_fix_spec.md` in place** — all three defects it documents are fixed at HEAD.
10. **Consider deleting** `streaming/async_pipeline.py` + `streaming/ramdisk.py` (checklist in `BUGS_FOUND.md`).

## Session Instructions
- **Four remediation-ordering constraints. Violating them makes things worse, not neutral** — all four are in `BUGS_FOUND.md` under "Remediation ordering":
  1. **#11 must land at all three sites in one commit** — `core/result.py:17`, `rust_ext/src/core/apriori.rs:73`, `synthetic.py:71-72`. They currently agree *because they are the same wrong expression*; a one-sided fix breaks the tier chain.
  2. **#4 must land before or with #17.** Fixing the MKL probe alone *activates* the float32 saturation for installs currently returning correct answers.
  3. **#35 must canonicalise to the *sorted* representative.** The word is load-bearing: collapsing to an arbitrary one drops 132 of 1,718 rules' lift from 1.0 to 0.0, because SON's duplicates are currently *masking* #2.
  4. **Fixing #13 turns the suite red, and that is correct.** `tests/test_streaming_bug.py:165` asserts the defect. Delete that assertion; do not revert the fix to get green.
- **#1's fix is three-part** and part 3 must be *additive*: do **not** remove the `sorted()` at `tests/test_tier_equivalence.py:44` (CLAUDE.md forbids weakening those assertions). Add a per-tier order assertion alongside it.
- Do **not** place #1's enforcement at `core/result.py::_build_result_df` — it is not the shared emission point it claims to be (that is finding #20).
- Reviewers were told to write scratch work to `/tmp` only and never modify repo files. Keep that.

## Decisions & Rationale
- **Three sequential councils, not one**: the council skill caps a target set at 25 files; ET-Miner is ~95 source files. User chose the full sweep over a single-subsystem review.
- **Review-only mode**: no fixes during the review, so a dirty tree could not sweep up `applications/` and `runs/`.
- **Emit-only is the unique fix for #22**: hoist+remap was proposed, simulated as exact (321/321), then *implemented for real* and measured to lose 129 of 321 — it repairs only the level immediately below the first filtered one. Rejected on measurement, not argument.
- **Deletion over fixing for the two dead streaming modules**: they carry 7 of the 62 defects and have zero importers; deleting closes all seven at zero behavioural risk.

## Dead Ends
- **Do not "fix" #22 by remapping anchors to the lowest column indices** (with or without hoisting `full_flat`): measured 129/321 lost. The apriori oracle needs the anchor-*dropping* subsets, which a restricted generation base never produces — no column ordering repairs it.
- **Do not widen the device guard in `_src/k3plus_fused.cu:30` from `< 62` to `< 64`**: it buys unreachable capacity at real risk. The host check is the whole fix (#25).
- **Do not fix #35 at `streaming/son.py:644`**: that is inside `_mine_chunk_frequent`, one of *two* pass-1 producers. `_mine_chunk_gpu_resident` builds tuples at `:530` and never reaches `:644`. Canonicalise at the funnel.
- **Do not set `CUDA_VISIBLE_DEVICES=0` when testing multi-GPU code**: `getDeviceCount()` returns 1 and the wrappers silently degrade to single-GPU through `n_gpus = min(n_gpus, available_gpus, ...)` at `kernels/k3plus.py:119-122`, `:373-374`, `gpu_resident.py:416-417`. This produced a false negative during the review. Assert `getDeviceCount() == 2` inside any multi-GPU script.
- **Do not verify an sm_90 build by passing `--gpu-architecture=compute_90` as a RawModule option**: CuPy appends its own `-arch=86` afterwards and NVRTC honours the last one. Read the arch back out of the cubin ELF header.

## Key Findings
- **#33 is the worst and is live on the default path.** `apriori_streaming_multi_gpu(n_gpus=2)` on 2×RTX 3090 returns support `0.6667` where the truth is `1.0`; `n_gpus=1` is exact. Reproduced 3× independently, including by me on the first attempt. Root cause: `gpu/bitvec.py:65` calls `build_bitvecs_gpu_from_scipy(csr)` with **no device argument**, and `gpu/csr_bitvec.py:243` then *explicitly overrides* the caller's device context with `with cp.cuda.Device(device_id)`, `device_id=0`. Error: `ValueError: The device where the array resides (0) is different from the current device (1). Peer access is unavailable between these devices.`
- **`docs/specs/et_miner_fix_spec.md` Defect A is FIXED at HEAD** — refuted by two reviewers independently, one at the spec's exact conditions (714,841 == 714,841, zero missing/extra/count-mismatch, under column permutation and every env knob in the spec's own ruled-out list). Commit `9ec6b8c` fixed it; its docstring at `gpu/row_split.py:96-101` describes the old failure verbatim. Defect B and the `level_callback` gap are also fixed.
- **The suite is green against all 62 defects in three structurally different ways**: no coverage; normalised away (`tests/test_tier_equivalence.py:44` canonicalises before comparing); pinned as expected (`tests/test_streaming_bug.py:165`). Neither streaming path is in CLAUDE.md's mandated tier chain at all.
- **CLAUDE.md's allowed-intrinsic list is incomplete** — `__ffsll` (`_src/bitvec_extract_tids.cu:19`, required: `__ffs` would truncate a 64-bit word) and `__popc` (10 of 16 `.cu` files) are both used and both absent. Doc fix, not a code fix.

## Blockers & Pending Decisions
- **User decision: adopt the deletion of `streaming/async_pipeline.py` + `streaming/ramdisk.py`?** Closes 7 defects; it is an *API removal* (both ship in the wheel with `__all__`), though neither is exported from any `__init__.py`. Optional 4-line `raise ImportError` shim for one release if the assumption is uncomfortable.
- **User decision: `mine_two_phase` needs redesigning or re-documenting.** #22's fix is correct but forfeits the feature's purpose — Phase 2 then mines the full lattice at `phase2_support=0.00001`. Someone who fixes only the correctness bug ships an intractable Phase 2. There is no known pruning-preserving fix.
- **Unverified, recorded as such, not as passing:** sm_90 *runtime* (compilation verified 19/19); real-bucket GCS pathways; #33 at N≥3; whether any *external* consumer imports the two dead modules.

## Test Status
`uv run pytest -q` (full, incl. slow): **479 passed, 6 skipped** — reviewer-run, unchanged at HEAD.
`uv run pytest -q -m "not slow"`: **471 passed, 4 skipped**. `tests/test_tier_equivalence.py`: **9/9**. `bench/selfcheck.py`: 19 kernels compiled on both devices, READY.
Not re-run by me after the report was written; no source changed.

## What Was Done
- Wrote `BUGS_FOUND.md` (3,524 lines) — 62 defects with `path:line`, trigger, impact, and either a pasted reproduction or the lines that prove it.
- Independently verified ~15 load-bearing claims by direct run, including #33's repro, the `"i_10" < "i_2"` ordering, `ceil(0.07*100)==8`, `panic = "abort"`, the two `s_items` caps, the zero-importer grep, and the `d=0`-drops-the-anchor mechanism.
- Caught and corrected 8 errors in my own draft after first calling it complete — a fix that would have made #25 worse, a chunk cap cited backwards, a misleading anchor headline, the wrong root-cause line for #33, and a fix site that missed one of two producers.

## Commits This Session
No commits — review-only, by user instruction.

## Key Files
| File | Role |
|------|------|
| `BUGS_FOUND.md` | The deliverable. 62 defects + negative results + retractions + remediation ordering |
| `docs/specs/et_miner_fix_spec.md` | **Stale** — all three defects fixed; amend in place |
| `src/et_miner/gpu/bitvec.py:65` | #33's one-line fix site |
| `src/et_miner/core/matrix.py:136` | #1's root cause (unpadded `i_{idx}` names) |
| `src/et_miner/core/rules.py:43` | #2's fix site (key the lookup sorted) |
| `src/et_miner/gpu/row_split.py:670-672` | #22's emit-only fix site |
| `src/et_miner/core/sparse.py:118-121` | #4's int32→float32 cast |
| `src/et_miner/streaming/son.py:298` | #35's fix site (the merge, not `:644`) |

## Extra Context
`fix all bugs starting with HIGH severity`

The 17 HIGH defects, grouped by fix:
- **§1 (6):** #1 emission order · #2 `generate_rules` key mismatch · #3 mixed-tier parquet joins · #4 float32 saturation · #5 `panic = "abort"` · #6 streaming drops `prune_equal_support`
- **§2 (3):** #22 `anchor_items` · #23 multi-GPU bare clamp · (#24 is MEDIUM)
- **§3 (8):** #33 multi-GPU wrong supports · #34 SON pass-1 swallow · #35 SON duplicate emission · #36 async threshold · #37 GCS name collision · #38 low-disk shadowing · #39 no completion marker · plus the low-disk-fallback single-copy finding

**#1, #2, #3 and #35 are one root cause** — fixing the emission order plus the sorted lookup closes four HIGHs. Start there and at #33; those two changes address 5 of the 17.

`BUGS_FOUND.md` marks every finding as measured or derived and keeps an explicit unverified list. Where a fix is validated end-to-end (#22 — six anchor × gate configs, the sparse branch, full suite green on a patched copy), the report says so and gives the acceptance criteria.
