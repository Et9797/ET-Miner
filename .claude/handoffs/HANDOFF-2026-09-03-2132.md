# Session Handoff — ET-Miner — 2026-09-03 21:32

**Branch:** `v2` @ `3c24718` (pushed to `origin/v2` at 21:33; tree clean)
**Focus:** V2 preprint of `paper/et_miner_proteome.tex` reporting only the fresh RTX 3090 rerun; next up is a citation audit before the PR.
**Reason:** end of session. Both open review questions are now decided (see Decisions); the citation audit has NOT started.

## What Was Done
- **V2 preprint complete and committed** (`3c24718`), now pushed to `origin/v2`. Zero H100/Hopper/SXM/SM90 hits in the tex AND in `pdftotext` output; latexmk rc=0, 13 pages, 1 pre-existing overfull box (author tabular, `paper/et_miner_proteome.tex:498-501`), no undefined refs/citations.
- **Discovered the single-GPU claim was false**, then fixed it by re-measuring: `src/et_miner/gpu/dispatch.py:128-129` (`CANDIDATE_COUNT_THRESHOLD_K3 = 500_000`) sends any K>=3 level with >=500,000 candidates to *all visible* GPUs; no launch script pinned a device. Re-ran the campaign and the direct-vs-SON comparison with `CUDA_VISIBLE_DEVICES=0` (`runs/20260902T0000Z/phase2/scripts/chain_single_gpu.sh`), sampling both GPUs every 5 s. GPU 1 idle in **all** samples (1,017 campaign / 1,244 comparison). → RESULTS `P-028`, `P-029`, `X-025`.
- **Timings replaced** with the pinned run: Opus 43.9 min (2,631.2 s), Ultra 33.3, Blitz 4.9, Power 74.7 s, Super 52.6 s, Base 31.1 s; direct 62.2 s vs SON 6,151.1 s = 99x (SON split: 88 min chunk-local + 14 min global re-count).
- **Appendix rewritten for the code path that actually ran** (`_apriori_from_bitvecs`): prefix groups built on host and uploaded, 12-byte survivor records downloaded per level (0.32 GB for Opus), host sort, tile-level early exit, 3.1 GB one-time CSR copy. The V1 appendix described `gpu_resident=True` (`src/et_miner/gpu/mining.py:824-977`), which no reported run used. → `X-023`, `X-024`.
- **Figures regenerated** from the pinned campaign JSON by `paper/figures/make_figures.py` (now also draws `architecture.pdf`, which previously embedded "GPU (H100 80 GB)"). → `F-002`.
- **New evidence rows**: `P-026` (example triple = 1,234 proteins), `P-027` (SON set identity at Base/Super: 0 differences), `P-030` (intermediate-K supports), `X-022` (InterPro2GO check).
- **`CHANGELOG_V1_V2.md`** (614 lines) generated by `runs/20260902T0000Z/phase4/make_changelog_v2.py`: 55 changed values with artifacts, removed claims, 53 ripple sentences, flagged items, and a traceability table asserting every numeric token in the body maps to a RESULTS row / COMPARISON_REPORT key / stated role.
- **Three fresh-context adversarial reviews** (tex+blog, code inspection of PCIe claims, final re-review). Final round found 10 defects, all fixed in `runs/20260902T0000Z/phase4/revise_tex_v2_pass7.py`.

## Commits This Session
- `3c24718` paper: V2 preprint reporting only the fresh RTX 3090 run — **pushed to origin/v2**
- (parent, earlier session) `08ce0e1` paper: revise tex and blog with the fresh base214m values; regenerate result figures

## Decisions & Rationale

**Decision 1 — Literature citations (verbatim from Et):**
> Literature citations: RE-CHECK, do not accept as-is. Every cited value and claim
> must be traced to the work it is attributed to. Anything that cannot be verified
> in the actual source gets deleted, not rephrased.
> Rationale: original run artifacts were lost; no number in the draft has trusted
> provenance.

**Decision 2 — K=13/K=11 pattern counts (verbatim from Et):**
> K=13/K=11 pattern counts: REPORT MEASURED MAXIMA from the 3090 rerun.
> [OVERRIDE HERE if pinning to itemsets] Whichever is chosen, state the convention
> explicitly in Methods so the two K values are defined the same way.
> Rationale: V2 reports only what this rerun measured.

Decision 2 status: the tex **already reports measured maxima** (`K=13` "up to 11,521 proteins", `K=11` "up to 28,913 proteins", both `paper/et_miner_proteome.tex:341-355`, RESULTS `P-030`), but the **convention is stated only in a footnote at `:341`, not in Methods** — step 4 of Next Steps closes that gap. Note the K=12 line currently uses a *different* convention ("up to 16,185 proteins; 111 itemsets at this level contain both domains" — a domain-filtered maximum), so Methods must define both conventions or K=12 must be aligned.

Earlier session decisions that still hold:
- **Single-GPU numbers must come from a pinned run**, not from the mixed-configuration campaign (P-014/P-010/P-013 are superseded for the paper by P-028/P-029 but stay in RESULTS as the two-GPU measurements).
- **SON set identity is claimed only where verified** (Base, Super); at Power the artifacts compare counts and K-distributions only.

## Key Findings
- **`\bibliographystyle{unsrt}` at `paper/et_miner_proteome.tex:530` is INERT.** The bibliography is a hand-written `\begin{thebibliography}{38}` (`:532`), so numbering follows `\bibitem` order, not citation order — `\bibliographystyle` only affects BibTeX-generated bibliographies. Fix = reorder the 38 `\bibitem` blocks into first-citation order (or move to a real `.bib` + bibtex run). **First mismatch at position 8**: first-citation order has `zhang2011`, `\bibitem` order has `han2000`. This is why the Introduction shows `zaki1997`=[36] and `miettinen2020`=[38] early. Verified: 38 cited, 38 bibitems, no orphans either way.
- **Rebuild needs a `binhex.tex` stub.** Debian TeX Live 2021's `newpxmath.sty` `\input{binhex}` but the file is not packaged; a one-line stub (`\def\nhex#1#2{}`) lives in the scratch build dir. Build command used:
  `cd paper && TEXINPUTS=<dir-with-binhex>: latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=<dir> et_miner_proteome.tex`
- Tool-comparison material to delete in step 2 (all of it exists only to carry other systems' numbers): §4.2 "Comparison with Prior GPU FIM Systems" (`:400-444`) incl. Table 6 `tab:scale-comparison` (`:404-427`); Appendix D Table 7 `tab:gpu-arch` (`:820-846`) and Table 8 `tab:gpu-pipeline` (`:849-871`); the "50--350x speedups" and "a few million real transactions" clauses in the Introduction (`:105`); the "compared to 32 with the 32-bit `__popc` used by prior systems" clause in Appendix C.1 (`:796`).
- Deleting those renumbers/orphans many `\bibitem`s — that is exactly why step 3 comes after step 2.
- `runs/20260902T0000Z/phase4/` holds every V2 edit script (`revise_tex_v2_pass{1,4,5,6,7}.py`, each asserting its anchors match exactly once) plus `make_changelog_v2.py`; re-run the changelog generator after any tex edit or its traceability assert will fail on new/orphaned tokens.

## Current State
Nothing is running. Working tree clean at `3c24718`, `v2` tracks `origin/v2`. `paper/et_miner_proteome.pdf` in git matches the current tex. The citation audit (steps 1-3) has not started — **no `\cite` has been verified against its source yet**. Both Table 6 and Appendix Tables 7-8 are still in the document.

## Blockers
- **Citation verification cannot be completed on this box.** No full-text access to the cited papers; only DOIs/titles are in the `\bibitem` blocks. Every value below is attributed to another work and could not be checked against that work in this session — under Decision 1 each must be traced or **deleted**:
  | value | attributed to | tex location |
  |---|---|---|
  | 50--350x speedups | luna2019, zhang2011, chon2018, djenouri2019, acmsurvey2021 | `:105` |
  | "at most a few million real transactions" / scale gap | same group | `:105`, `:402` |
  | GPApriori up to 100x | zhang2011 | `:402` |
  | GMiner 15M synthetic / 1.7M real / 20K items / K~30 / 4x GTX 1080 / 20--150 s | chon2018 | `:402`, `:416`, `:820` |
  | GMiner++ pre-calculated bit arrays, 32-bit `__popc` | chon2024 | `:402`, `:832`, `:796` |
  | BIGMiner 100M txns / 100K items / 30 servers / 1--20K s | chon2018b | `:402`, `:417` |
  | Borgelt 100K / 500 / K~10 / 1x CPU / 1--100 s | borgelt2003 | `:414` |
  | Fang 100K / 1K / K~5 / 1x GTX 280 / 1--10 s | fang2009 | `:402`, `:415` |
  | CGSS GPU+cluster | djenouri2019 | `:402` |
  | "prior domain co-occurrence limited to K=2" | wang2011, coin2009 | `:101`, `:448` |
  | "~32K structures" | meysman2015 | `:448` |
  | 1.7M high-confidence + 18M lower-confidence homodimers; "EMBL-EBI, 2026" | abramson2024 / unattributed | `:480` |
  | "GO annotations are updated monthly" | go2023 (implicit) | `:472` |
  | `__popcll` "single clock cycle" | unattributed hardware claim | `:796` |
  | "5.1x more transactions than the largest prior single-machine GPU system" | derived from chon2018 | `:405` |
- Steps 2-3 are mechanical once step 1 produces the verified/unverifiable split; no other blocker.

## Next Steps
1. **Citation audit, pass 1 — verify.** For every `\cite` in the V2 tex, confirm the source exists and supports the sentence. Produce the unverifiable list BEFORE editing anything (start from the Blockers table).
2. **Citation audit, pass 2 — remove** all literature values describing other systems/tools; delete the whole sentence where it exists only to carry the comparison. Targets listed under Key Findings.
3. **Fix reference ordering (AFTER step 2).** Reorder the 38 `\bibitem` blocks to first-citation order — `\bibliographystyle{unsrt}` does nothing here because the bibliography is hand-written. Then `latexmk -C` (or delete `.aux`/`.bbl`/`.out`) and do a full rebuild; stale aux files reproduce the old numbering.
4. **Apply Decision 2**: state the K=13/K=11 (and K=12) counting convention explicitly in Methods, not just in the footnote at `:341`; make the two K values use the same convention.
5. **Rebuild the PDF**; confirm zero undefined references and no `??` in the output; re-run `runs/20260902T0000Z/phase4/make_changelog_v2.py` so the traceability assert covers the edited text; copy the built PDF over `paper/et_miner_proteome.pdf`.
6. **Commit, push `v2`, open the PR** — base `alphafold-experimental-results-reproduction`, do NOT merge, leave open for review.

## Key Files
| File | Role |
|------|------|
| `paper/et_miner_proteome.tex` | the V2 preprint (bibliography at `:530-724`, §4.2 at `:400`, appendix tables at `:820`, `:849`) |
| `paper/et_miner_proteome.pdf` | committed build, matches the tex |
| `CHANGELOG_V1_V2.md` | V1→V2 value map, removed claims, flagged items, traceability table |
| `RESULTS.md` | fresh values with artifact paths; V2 rows `P-021`..`P-030`, `X-017`..`X-025`, `F-001`, `F-002` |
| `COMPARISON_REPORT.md` | §2 headline table (qkey → fresh value → artifact), §4.tex per-claim verdicts |
| `runs/20260902T0000Z/phase3/2026_01/single_gpu/` | pinned rerun: campaign + direct-vs-SON JSONs, logs, nvidia-smi CSVs |
| `runs/20260902T0000Z/phase4/revise_tex_v2_pass*.py` | the V2 edit scripts (anchor-asserting) |
| `runs/20260902T0000Z/phase4/make_changelog_v2.py` | regenerates `CHANGELOG_V1_V2.md`; asserts full numeric traceability |
| `runs/20260902T0000Z/phase2/scripts/chain_single_gpu.sh` | pinned-rerun driver (`CUDA_VISIBLE_DEVICES=0` + nvidia-smi sampling) |
| `runs/20260902T0000Z/phase2/scripts/smi_summary.py` | summarizes the nvidia-smi CSVs per GPU |
| `paper/figures/make_figures.py` | regenerates all three data-bearing figures from the campaign JSON |
| `PROGRESS.md` / `CLAUDE.md` | durable campaign state; CLAUDE.md carries the operating rules + the dispatch warning |

## Extra Context

**PR requirements (from Et, verbatim):**
- Base branch: `alphafold-experimental-results-reproduction`. NOT main.
- Do not merge. Leave it open for my review.
- Description must list: citations removed, citations that failed verification, and the K=13/K=11 convention chosen.

**Constraints (from Et, verbatim):**
- Every number in V2 must trace to a 3090 rerun artifact. No artifact means flag it, not keep it.
- No H100 references anywhere in the draft.
- Do not touch main.

**Environment notes:** `.venv/bin/python` (uv); TeX Live 2021 + latexmk 4.76 installed via apt this session; `pdftoppm`/`pdftotext` available via poppler-utils. GPUs idle. The scratch build dir with the `binhex.tex` stub is session-local — recreate the stub before the first rebuild.
